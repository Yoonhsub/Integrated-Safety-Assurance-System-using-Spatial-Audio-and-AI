# 김도성_디버그 리포트

> 이 문서는 김도성 담당 AI 에이전트가 섹션 3, 5, 7, 9, 11의 **검토/패치 단계**에서 반드시 최신화해야 하는 디버그 리포트이다.  
> 이 문서는 실제 개발 완료 보고서가 아니라, 각 검토 단계에서 발견한 문제·패치 내역·미해결 이슈를 추적하기 위한 문서이다.

---

## 1. 담당 범위

- 담당 팀원: 김도성
- 담당 역할: 공공데이터 API 기반 버스 정보 연동, 저상버스 필터링, 표준 응답 JSON/mock 데이터, AI 비전 데이터 수집 계획 및 모델 리서치
- 주요 소유 경로: `services/public_data, ai_vision`
- 직접 담당하지 않는 범위: 비콘 거리 테스트 계획, 공간음향 방향 테스트 계획, FastAPI 백엔드, Firebase, FCM, Flutter UI, BLE/RSSI, 헤드트래킹

---

## 2. 작성 원칙

```txt
1. 섹션 3, 5, 7, 9, 11이 끝날 때마다 반드시 최신화한다.
2. 이전 구현 섹션의 결과물을 먼저 검토한다.
3. 이상이 있으면 해당 부분만 패치한다.
4. 이상이 없으면 "다음 섹션으로 넘어가도 된다"고 사용자에게 보고한다.
5. 다음 섹션으로 임의 점프하지 않는다.
6. 충돌 가능성이 있으면 docs/rw/충돌 이슈.md에 기록하고 개발을 중단한다.
7. docs/rw/공통 진행사항.md의 자기 팀원 기록 공간도 함께 최신화한다.
8. 다른 팀원의 디버그 리포트를 수정하지 않는다.
```

---

## 3. 공통 검토 체크리스트

- [ ] 공공데이터 API 조사 문서 정합성
- [ ] 정류장별 버스 도착 정보 조회 모듈
- [ ] 실시간 버스 위치 정보 제공 가능성 조사 및 구현 여부
- [ ] 저상버스 여부 boolean 표준화
- [ ] 혼잡도 LOW/NORMAL/HIGH/UNKNOWN 표준화
- [ ] 표준 응답 JSON 구조
- [ ] mock 응답 JSON 제공 여부
- [ ] 심현석 백엔드 수신 인터페이스와의 정합성
- [ ] AI 비전 데이터 수집 계획 및 모델 리서치 문서

---

## 4. 디버그 기록 양식

아래 양식을 복사하여 최신 기록을 아래쪽에 누적한다.  
기록은 삭제하지 않고, 해결된 문제는 해결 상태를 갱신한다.

```md
### DEBUG-김도성-0000

- 기록자: 김도성의 에이전트
- 기록일시:
- 검토 섹션:
- 검토 대상 이전 구현 섹션:
- 검토 대상 파일/폴더:
- 검토 목적:
- 실행한 검증 방법:
- 발견한 문제:
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [ ] 보통
  - [ ] 낮음
  - [ ] 단순 문서 보완
- 패치 여부:
  - [ ] 패치 완료
  - [ ] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
- 수정한 파일:
- 남은 문제:
- 관련 충돌 이슈:
- docs/rw/공통 진행사항.md 최신화 여부:
- 다음 섹션 진행 가능 여부:
- 사용자에게 전달한 보고 문구:
```

---

## 5. 디버그 기록

### DEBUG-김도성-0001

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 06일 21시 05분 (KST)
- 검토 섹션: 섹션 3
- 검토 대상 이전 구현 섹션: 섹션 2 (공공데이터 API 후보 조사·표준 mock 기준 확정)
- 검토 대상 파일/폴더:
  - `services/public_data/README.md` ("공공데이터 API 조사 결과(섹션 2)" 절 8개)
  - `services/public_data/examples/mock_bus_arrivals.json` (4건으로 다양화)
  - `docs/rw/선행작업의존성 정리.md` (DEP-APR-001 김도성 섹션 2 상태)
  - `docs/rw/공통 진행사항.md` (김도성 기록 0002)
- 검토 목적:
  - 섹션 2 산출물이 (1) shared schema와 정합한지, (2) 1차 출처 기반인지, (3) 원본 필드명과 표준 필드명을 분리하는지, (4) 김도성 담당 외 영역에 침범 없는지 확인.
- 실행한 검증 방법:
  - 검수 1: mock_bus_arrivals.json ↔ shared schema 핵심 제약 stdlib 재검증 + 다양성 분포 검사 (congestion 4종/lowFloor true·false/remainingStops null·0·정수/arrivalMinutes 0·일반·큰 값).
  - 검수 2: mock JSON 키와 README 본문에서 비계약 필드(stopName, source, rawData, busType, reride_Num 등) 침투 검사.
  - 검수 3: README 내 모든 URL을 추출해 공식 출처(data.go.kr, bus.go.kr, gbis.go.kr, data.seoul.go.kr) 비율과 "확인 필요" 표기 위치, 단정형 위험 표기 검사.
  - 검수 4: 선행작업의존성 정리.md에서 김도성 섹션 2 외 다른 섹션/의존성 ID 상태 변경 여부 확인 + DEP-APR-001 4개 헤더 무결성 확인.
  - 검수 5: 김도성 담당 외 132개 파일의 mtime이 모두 압축 해제 시점(2026-05-06 11:42)인지 확인하여 무단 수정 없음 검증.
  - 검수 6: schemas.py(Pydantic) ↔ shared schema(JSON Schema) ↔ mock JSON 3자 정합성. 필수 필드 집합 일치 여부, mock의 모든 케이스가 Pydantic 제약(extra=forbid, ge=0, enum) 만족 여부.
  - 부가: 전체 JSON parse 17/0 PASS, Python AST parse 30/30 PASS.
- 발견한 문제:
  - **검수 1·2·3·4·5·6 모두 PASS — 섹션 2 산출물에 패치할 부분 없음.**
  - 다만 다음 항목들은 "표현 명확성 향상 후보" 또는 "이월 후보"로 식별됨:
    1. (표현 명확성 후보) `services/public_data/README.md` §섹션 4 저상버스 매핑 표 본문 L116-117에서 "굴절(2)인 경우 lowFloor=false"와 "vehicletp 미제공 시 lowFloor=False로 단정하지 않음"이 인접한 두 줄에 배치되어 처음 읽는 사람이 lowFloor 기본값을 false로 오해할 가능성. 두 줄 모두 의미는 일관됨(굴절은 단정 가능, 미제공은 단정 회피)이므로 섹션 2 패치가 잘못된 것이 아님 — **섹션 6에서 lowFloor 미상 처리 정책 확정 시 함께 명확화하는 후보**로 둠.
    2. (이월) `data_go_kr_client.py`에 `PUBLIC_DATA_CITY_CODE`/`PUBLIC_DATA_USE_MOCK` 미사용 — README에는 "섹션 4에서 통합"이라고 명시 → 섹션 4 범위.
    3. (이월) `bus_arrivals_service.py`의 mock/real provider 경계 미명시 — 섹션 4 범위.
    4. (이월) TAGO `ArvlInfoInqireService` vehicletp/혼잡도 필드 명세는 활용신청 명세서 직접 확인 필요 — README "확인 필요" 표기됨, 섹션 4(클라이언트) 또는 섹션 6(normalize) 범위.
    5. (이월) `low_floor_filter.prioritize_low_floor`는 단순 정렬만 — 섹션 6의 "저상버스 우선 정렬 또는 필터링 로직" 범위.
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [ ] 보통
  - [ ] 낮음
  - [x] 단순 문서 보완 (위 후보 1건은 섹션 6에서 함께 다루기 좋은 표현 명확성 항목)
- 패치 여부:
  - [ ] 패치 완료
  - [x] 패치 불필요 (섹션 2 산출물 자체에 잘못된 패치 없음)
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
  - 없음. 검토 섹션의 "원칙적으로 새 ZIP을 만들지 않는다. 단, 섹션 2 패치가 잘못되어 있으면 해당 부분만 보정할 수 있다" 규정에 따라, 잘못된 패치가 식별되지 않았으므로 섹션 2 산출물에 대한 보정은 수행하지 않았다.
- 수정한 파일:
  - `docs/rw/김도성_디버그 리포트.md` (본 기록 추가)
  - `docs/rw/공통 진행사항.md` (김도성 기록 공간에 기록 0003 추가)
- 남은 문제:
  - 위 "발견한 문제"의 후보 1~5는 모두 섹션 4 또는 섹션 6의 명시 목표 범위에 해당하므로, 해당 섹션 진입 시 처리한다.
- 관련 충돌 이슈: 없음.
- docs/rw/공통 진행사항.md 최신화 여부: 김도성 기록 0003으로 최신화 예정.
- 다음 섹션 진행 가능 여부: **가능**. 섹션 4(DataGoKrClient·버스 도착 조회 skeleton 보강) 진입 가능. 단, 사용자의 명시적 지시(`섹션 4` 또는 `다음 섹션`) 없이 임의 진행하지 않음.
- 사용자에게 전달한 보고 문구:
  - **지금 섹션 3 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

---

### DEBUG-김도성-0002

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 07일 18시 17분 (KST)
- 검토 섹션: 섹션 5
- 검토 대상 이전 구현 섹션: 섹션 4 (DataGoKrClient·BusArrivalsService·예외 계층 보강)
- 검토 대상 파일/폴더:
  - `services/public_data/public_data_client/exceptions.py` (신규)
  - `services/public_data/public_data_client/data_go_kr_client.py` (수정)
  - `services/public_data/public_data_client/bus_arrivals_service.py` (수정)
  - `services/public_data/public_data_client/__init__.py` (수정)
  - `services/public_data/public_data_client/low_floor_filter.py` (수정 1줄)
- 검토 목적:
  - 환경변수 분기 의도와 실제 동작 일치, 예외 계층/raise 시점 정상성, transport vs service 책임 분리 위반 검사, 부작용/누수 위험 검사, 호환성/정합성 재확인.
- 실행한 검증 방법:
  - 검수 1: `_is_mock_mode()` edge cases 6개 추가 (False/공백 trim/Yes/OFF/disabled/enabled) + 정책 문서(`ENVIRONMENT_VARIABLES.md`/`.env.example`/`README.md`) 일관성 4건.
  - 검수 2: AST 파싱으로 4개 예외 클래스 상속 구조 확인 + raise 시점 모든 위치 추출 + `PublicDataEmptyResponseError` 사용 분석 (8회 등장, raise 0회 — 섹션 6 예정).
  - 검수 3: `data_go_kr_client.py`의 표준 모델 참조(false positive — docstring일 뿐) 확인 + `bus_arrivals_service.py`의 `httpx` 직접 사용 확인 + `BusArrivalsService.get_arrivals` 분기 검증.
  - 검수 4: `LiveBusArrivalsProvider`/`BusArrivalsService` `__init__` 부작용 시뮬레이션 — Fake 클래스로 ctor count 추적.
  - 검수 5: `self.client`/`self.live_provider` 사용처 전수 검색하여 lazy property 호환성 검증.
  - 검수 6: 패치 후 AST/JSON parse, `__init__.py` export 12개, shared schema↔mock JSON 정합, 김도성 외 영역 mtime 무수정 (정확한 threshold로 0건 확인).
  - 부가: lazy 동작 시뮬레이션 (Fake로 A~F 6 케이스 — mock 모드에서 ctor 0건/접근 시점 1건 생성/캐시/inject 우선) PASS.
- 발견한 문제:
  - **검수 4에서 섹션 4 패치 잘못 1건 발견(보통 심각도)**:
    - `LiveBusArrivalsProvider.__init__`이 `client or DataGoKrClient()` 패턴을 사용 → `BusArrivalsService` 생성 시 `LiveBusArrivalsProvider` 즉시 생성 → `DataGoKrClient` 즉시 생성 → `httpx.Client` 즉시 생성. 결과적으로 mock 모드에서도 사용되지 않을 `httpx.Client`가 매번 생성되어 file descriptor 누수 위험.
    - 섹션 4의 `BusArrivalsService.__init__` 주석에 "인스턴스 생성 자체는 부작용이 없으므로 OK"라고 적었으나 실제로는 부작용이 있어 주석과 동작이 불일치 → 검토 섹션의 "섹션 2 패치가 잘못되어 있으면 해당 부분만 보정할 수 있다" 규정에 명확히 해당하여 패치함.
  - **검수 2의 인지 사항(패치 불필요)**:
    - `PublicDataEmptyResponseError`가 정의/export/import는 되어 있지만 raise 위치는 0회. 정적 분석 도구가 unused warning을 낼 가능성. 섹션 4에서 의도적으로 "섹션 6에서 빈 응답 정책 확정 후 raise 예정"으로 둔 것이며 docstring에 명시되어 있어 패치 잘못이 아님 — 인지 기록만 남김.
  - **검수 1·3·5·6은 모두 PASS — 추가 패치 없음.**
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [x] 보통 (mock 모드 자원 누수, 4월 단계 즉시 영향은 작지만 운영 단계에서 file descriptor 누수 위험)
  - [ ] 낮음
  - [ ] 단순 문서 보완
- 패치 여부:
  - [x] 패치 완료
  - [ ] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
  - `services/public_data/public_data_client/bus_arrivals_service.py`:
    1. `LiveBusArrivalsProvider.__init__`을 lazy 생성으로 변경. `self.client = client or DataGoKrClient()` → `self._client_override = client; self._lazy_client = None`. 새로 `_get_client()` 메서드 추가, `client`를 `@property`로 노출하여 외부 인터페이스 호환 유지.
    2. `BusArrivalsService.__init__`도 동일 패턴 적용. `self.live_provider = live_provider or LiveBusArrivalsProvider()` → `self._live_provider_override = live_provider; self._lazy_live_provider = None`. `live_provider`를 `@property`로 노출.
    3. `BusArrivalsService.__init__`의 잘못된 주석("인스턴스 생성 자체는 부작용이 없으므로 OK") 제거하고 lazy 의도를 정확히 설명하는 주석으로 교체.
- 수정한 파일:
  - `services/public_data/public_data_client/bus_arrivals_service.py` (lazy 생성 패치)
  - `docs/rw/김도성_디버그 리포트.md` (본 기록)
  - `docs/rw/공통 진행사항.md` (김도성 기록 0005)
- 남은 문제:
  - `PublicDataEmptyResponseError`의 raise 위치는 섹션 6 normalize 정책 확정 시 추가 예정 (현재 인지 기록만).
  - 섹션 4 이월 사항(LiveBusArrivalsProvider 실제 호출/normalize, low_floor_filter 본격 구현, README §4 표현 명확화, TAGO vehicletp/혼잡도 매핑 명세 직접 확인)은 모두 섹션 6 범위 유지.
- 관련 충돌 이슈: 없음. 패치는 김도성 영역 내부 + 외부 인터페이스 property로 호환 유지 → 다른 팀 영향 0건.
- docs/rw/공통 진행사항.md 최신화 여부: 김도성 기록 0005로 최신화 예정.
- 다음 섹션 진행 가능 여부: 가능. 섹션 6(LiveBusArrivalsProvider 본격 구현·normalize·저상버스 우선·혼잡도 매핑) 진입 가능.
- 사용자에게 전달한 보고 문구:
  - **지금 섹션 5 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

---

## 6. 섹션별 검토 기준

### 섹션 3 검토 기준

- 섹션 2 구현 결과가 담당 범위와 일치하는지 확인한다.
- 기본 프로젝트 구조, 초기 설정, 인터페이스가 깨지지 않았는지 확인한다.
- 다른 팀원 소유 경로를 침범하지 않았는지 확인한다.

### 섹션 5 검토 기준

- 섹션 4 구현 결과의 주요 로직을 검토한다.
- API, UI, 센서, 데이터 표준화 등 담당 영역별 핵심 기능이 문서와 일치하는지 확인한다.
- 발견된 문제는 가능한 한 해당 담당 영역 내부에서만 패치한다.

### 섹션 7 검토 기준

- 섹션 6 구현 결과의 예외 처리, 상태값, mock/테스트 흐름을 검토한다.
- 충돌 가능성이 있는 인터페이스 변경이 있었는지 확인한다.
- 필요한 경우 docs/rw/충돌 이슈.md에 기록한다.

### 섹션 9 검토 기준

- 섹션 8 구현 결과가 다른 모듈과 연동 가능한 형태인지 확인한다.
- API 계약, JSON 필드명, 상태값, 파일 경계가 문서와 일치하는지 확인한다.

### 섹션 11 검토 기준

- 섹션 10 구현 결과를 최종 구현 전 단계 수준으로 검토한다.
- 섹션 12 최종 심층검증에 들어가기 전에 남은 문제를 정리한다.
- 미해결 이슈가 있으면 명확히 표시한다.

---

## 7. 초기 상태

```txt
문서 생성 상태: 초기 템플릿 생성 완료
등록된 디버그 기록: 없음
사용 시작 시점: 김도성의 에이전트가 섹션 3 검토/패치 단계에 진입하는 시점
```
