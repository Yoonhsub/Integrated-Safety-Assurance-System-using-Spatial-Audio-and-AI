# 심현석_디버그 리포트

> 이 문서는 심현석 담당 AI 에이전트가 섹션 3, 5, 7, 9, 11의 **검토/패치 단계**에서 반드시 최신화해야 하는 디버그 리포트이다.  
> 이 문서는 실제 개발 완료 보고서가 아니라, 각 검토 단계에서 발견한 문제·패치 내역·미해결 이슈를 추적하기 위한 문서이다.

---

## 1. 담당 범위

- 담당 팀원: 심현석
- 담당 역할: FastAPI 백엔드, Firebase Realtime Database, 지오펜싱, FCM, 기사-승객 매칭 데이터 파이프라인
- 주요 소유 경로: `backend/api, infrastructure/firebase, packages/shared_contracts 중 백엔드 계약 관련 영역`
- 직접 담당하지 않는 범위: 공공데이터 API 직접 구현, Flutter UI, BLE/RSSI, AI 비전, 헤드트래킹

---

## 2. 작성 원칙

```txt
1. 섹션 3, 5, 7, 9, 11이 끝날 때마다 반드시 최신화한다.
2. 이전 구현 섹션의 결과물을 먼저 검토한다.
3. 이상이 있으면 해당 부분만 패치한다.
4. 이상이 없으면 "다음 섹션으로 넘어가도 된다"고 사용자에게 보고한다.
5. 다음 섹션으로 임의 점프하지 않는다.
6. 충돌 가능성이 있으면 docs/rw/충돌 이슈.md에 기록하고 개발을 중단한다.
7. docs/rw/공통 진행사항.md의 자기 팀원 기록 공간도 함께 최신화한다.
8. 다른 팀원의 디버그 리포트를 수정하지 않는다.
```

---

## 3. 공통 검토 체크리스트

- [ ] FastAPI 서버 실행 및 라우트 연결 상태
- [ ] Firebase Admin SDK 연결 및 Realtime Database 경로 정합성
- [ ] geofence/check 요청·응답 구조
- [ ] 지오펜싱 상태값 SAFE/WARNING/DANGER/OUT_OF_AREA/UNKNOWN
- [ ] FCM 토큰 저장 및 푸시 발송 흐름
- [ ] rideRequests 생성·조회·상태 변경 흐름
- [ ] 김도성 공공데이터 표준 JSON 수신 인터페이스
- [ ] 윤현섭 Flutter 앱이 소비할 API 응답 계약

---

## 4. 디버그 기록 양식

아래 양식을 복사하여 최신 기록을 아래쪽에 누적한다.  
기록은 삭제하지 않고, 해결된 문제는 해결 상태를 갱신한다.

```md
### DEBUG-심현석-0000

- 기록자: 심현석의 에이전트
- 기록일시:
- 검토 섹션:
- 검토 대상 이전 구현 섹션:
- 검토 대상 파일/폴더:
- 검토 목적:
- 실행한 검증 방법:
- 발견한 문제:
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [ ] 보통
  - [ ] 낮음
  - [ ] 단순 문서 보완
- 패치 여부:
  - [ ] 패치 완료
  - [ ] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
- 수정한 파일:
- 남은 문제:
- 관련 충돌 이슈:
- docs/rw/공통 진행사항.md 최신화 여부:
- 다음 섹션 진행 가능 여부:
- 사용자에게 전달한 보고 문구:
```

---

## 5. 디버그 기록

### DEBUG-심현석-0001

- 기록자: 심현석의 에이전트
- 기록일시: 2026년 5월 6일 19시 53분 KST
- 검토 섹션: 섹션 3
- 검토 대상 이전 구현 섹션: 섹션 2
- 검토 대상 파일/폴더:
  - `backend/api/app/main.py`
  - `backend/api/app/services/firebase_client.py`
  - `backend/api/tests/test_health.py`
  - `backend/api/README.md`
  - `.env.example`
  - `backend/api/requirements.txt`
- 검토 목적: FastAPI·Firebase 백엔드 기초 구조가 섹션 2 목표와 일치하며, 인증정보가 없어도 import/test가 실패하지 않고, 타 팀원 영역을 침범하지 않았는지 확인
- 실행한 검증 방법:
  - `PYTHONDONTWRITEBYTECODE=1 python scripts/validate_architecture.py`
  - `cd backend/api && PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests -q -p no:cacheprovider`
  - Firebase/공유 API JSON 파일 파싱 확인
  - Python compile 확인
  - 섹션 2 수정 파일 정적 리뷰
- 발견한 문제: 없음
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [ ] 보통
  - [ ] 낮음
  - [x] 단순 문서 보완
- 패치 여부:
  - [ ] 패치 완료
  - [x] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용: 코드 패치 없음. 섹션 3 검토 결과를 디버그 리포트와 공통 진행사항에 기록함
- 수정한 파일:
  - `docs/rw/심현석_디버그 리포트.md`
  - `docs/rw/공통 진행사항.md`
- 남은 문제: 섹션 10~11 진입 전 김도성 public_data 표준화 산출물 재확인 필요
- 관련 충돌 이슈: 없음
- docs/rw/공통 진행사항.md 최신화 여부: 완료
- 다음 섹션 진행 가능 여부: 가능
- 사용자에게 전달한 보고 문구: 섹션 2 산출물은 현재 기준 문제 없으며, 섹션 4로 넘어가도 된다.


### DEBUG-심현석-0002

- 기록자: 심현석의 에이전트
- 기록일시: 2026년 05월 07일 11시 14분 KST
- 검토 섹션: 섹션 5
- 검토 대상 이전 구현 섹션: 섹션 4
- 검토 대상 파일/폴더:
  - `backend/api/app/api/routes/geofence.py`
  - `backend/api/app/schemas/geofence.py`
  - `backend/api/app/services/geofence_service.py`
  - `backend/api/tests/test_api_contract_validation.py`
  - `backend/api/README.md`
  - `packages/shared_contracts/api/geofence_check.request.schema.json`
  - `packages/shared_contracts/api/geofence_check.response.schema.json`
  - `infrastructure/firebase/realtime_database.schema.json`
- 검토 목적: 섹션 4 지오펜싱 API가 shared schema, Firebase RTDB 구조, 상태 전이 요구사항, FCM/Flutter 경계와 충돌하지 않는지 확인
- 실행한 검증 방법:
  - `PYTHONDONTWRITEBYTECODE=1 python scripts/validate_architecture.py`
  - `cd backend/api && PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests -q -p no:cacheprovider`
  - geofence request/response schema와 Python Pydantic 모델 필드 비교
  - Firebase RTDB 공식 경로(`/users/{userId}/currentLocation`, `/geofences/{stopId}`, `/systemLogs/{logId}`) 정적 검토
  - FCM/Flutter/공공데이터 영역 침범 여부 확인
- 발견한 문제:
  - 동일 사용자·정류장이 같은 `WARNING`/`DANGER`/`OUT_OF_AREA` 상태로 반복 체크될 때도 `/systemLogs` 이벤트가 계속 생성될 수 있었다.
  - `DANGER` 상태 이벤트가 `level=INFO`로 기록될 수 있어 위험도 표현이 부정확했다.
  - `docs/rw/선행작업의존성 정리.md`의 3단계 요약에는 심현석 섹션 4,5가 여전히 묶어서 `(미구현)`으로 남아 있어 DEP-APR-002 상세 블록과 부분적으로 불일치했다.
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [x] 보통
  - [ ] 낮음
  - [ ] 단순 문서 보완
- 패치 여부:
  - [x] 패치 완료
  - [ ] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
  - 최초 경고 상태 또는 이전 상태와 현재 상태가 달라지는 경우에만 상태 전이 이벤트를 생성하도록 `_should_create_event()`를 보정했다.
  - `DANGER` → `ERROR`, `WARNING`/`OUT_OF_AREA` → `WARNING`, 그 외 전이 → `INFO`로 이벤트 level을 매핑하는 `_event_level_for()`를 추가했다.
  - 동일 alert 상태 반복 호출 시 두 번째 응답의 `eventId`가 `None`이고 systemLogs가 중복 생성되지 않는 테스트를 추가했다.
  - `DANGER` 이벤트 level이 `ERROR`인지 확인하는 테스트를 추가했다.
  - `backend/api/README.md`와 선행작업의존성 문서를 섹션 5 기준으로 최신화했다.
- 수정한 파일:
  - `backend/api/app/services/geofence_service.py`
  - `backend/api/tests/test_api_contract_validation.py`
  - `backend/api/README.md`
  - `docs/rw/공통 진행사항.md`
  - `docs/rw/선행작업의존성 정리.md`
  - `docs/rw/심현석_디버그 리포트.md`
- 남은 문제:
  - 실제 FCM 알림 전송은 섹션 6 범위로 남아 있다.
  - 최근 geofence status를 영속 저장하는 별도 top-level path는 공식 schema에 없으므로 추가하지 않았다. 필요 시 별도 충돌 이슈와 schema 변경 협의가 필요하다.
- 관련 충돌 이슈: 없음
- docs/rw/공통 진행사항.md 최신화 여부: 완료
- 다음 섹션 진행 가능 여부: 가능
- 사용자에게 전달한 보고 문구: 섹션 4 산출물을 재검토했고, 중복 이벤트 생성과 위험도 level 표현 문제를 지오펜싱 범위 안에서 패치했다. 섹션 6으로 넘어가도 된다.


### DEBUG-심현석-0003

- 기록자: 심현석의 에이전트
- 기록일시: 2026년 05월 08일 13시 59분 KST
- 검토 섹션: 섹션 7
- 검토 대상 이전 구현 섹션: 섹션 6
- 검토 대상 파일/폴더:
  - `backend/api/app/api/routes/notifications.py`
  - `backend/api/app/schemas/notification.py`
  - `backend/api/app/services/fcm_service.py`
  - `backend/api/tests/test_api_contract_validation.py`
  - `backend/api/README.md`
  - `packages/shared_contracts/api/notification.request.schema.json`
  - `packages/shared_contracts/api/notification.response.schema.json`
  - `docs/rw/DATA_SCHEMA.md`
- 검토 목적: 섹션 6 FCM 토큰·알림 구조가 공식 RTDB 경로, shared notification schema, 사용자/기사 target 분리, mock 전송 테스트 요구사항과 일치하는지 확인
- 실행한 검증 방법:
  - `PYTHONDONTWRITEBYTECODE=1 python scripts/validate_architecture.py`
  - `cd backend/api && PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests -q -p no:cacheprovider`
  - notification request/response schema와 Pydantic 모델 필드 비교
  - `/fcmTokens/{ownerType}/{ownerId}` 외 중복 token path 생성 여부 정적 검토
  - rideRequests 섹션 8 범위 과구현 여부 확인
- 발견한 문제:
  - 구조적 오류는 없었다.
  - 다만 one-target 규칙과 notification `data` payload의 string-only 규칙은 shared schema상 중요하므로 회귀 방지를 위해 테스트 보강이 필요했다.
- 문제 심각도:
  - [ ] 치명적
  - [ ] 높음
  - [ ] 보통
  - [x] 낮음
  - [ ] 단순 문서 보완
- 패치 여부:
  - [x] 패치 완료
  - [ ] 패치 불필요
  - [ ] 충돌 가능성으로 패치 보류
  - [ ] 사용자/팀원 확인 필요
- 패치한 내용:
  - `targetUserId`와 `targetDriverId`가 동시에 들어오거나 둘 다 없는 알림 요청을 422로 거부하는 테스트를 추가했다.
  - `data` payload 값이 문자열이 아닐 경우 422로 거부하는 테스트를 추가했다.
  - `backend/api/README.md`에 섹션 7 검토 결과와 확정된 FCM 경계 조건을 기록했다.
- 수정한 파일:
  - `backend/api/tests/test_api_contract_validation.py`
  - `backend/api/README.md`
  - `docs/rw/공통 진행사항.md`
  - `docs/rw/심현석_디버그 리포트.md`
- 남은 문제:
  - 실제 Firebase Messaging 전송은 인증정보와 `FCM_ENABLED=true`가 있는 환경에서만 확인 가능하다. 현재 섹션에서는 mock 전송을 공식 개발/테스트 경로로 유지한다.
  - rideRequests 생성 시 기사 알림 helper 연결은 섹션 8 범위로 남겨 둔다.
- 관련 충돌 이슈: 없음
- docs/rw/공통 진행사항.md 최신화 여부: 완료
- 다음 섹션 진행 가능 여부: 가능
- 사용자에게 전달한 보고 문구: 섹션 6 산출물을 재검토했고, FCM 구조는 공식 경로·mock 전송·사용자/기사 target 분리 원칙과 일치한다. 회귀 방지 테스트와 문서 기록을 보강했으므로 섹션 8로 넘어가도 된다.


---

## 6. 섹션별 검토 기준

### 섹션 3 검토 기준

- 섹션 2 구현 결과가 담당 범위와 일치하는지 확인한다.
- 기본 프로젝트 구조, 초기 설정, 인터페이스가 깨지지 않았는지 확인한다.
- 다른 팀원 소유 경로를 침범하지 않았는지 확인한다.

### 섹션 5 검토 기준

- 섹션 4 구현 결과의 주요 로직을 검토한다.
- API, UI, 센서, 데이터 표준화 등 담당 영역별 핵심 기능이 문서와 일치하는지 확인한다.
- 발견된 문제는 가능한 한 해당 담당 영역 내부에서만 패치한다.

### 섹션 7 검토 기준

- 섹션 6 구현 결과의 예외 처리, 상태값, mock/테스트 흐름을 검토한다.
- 충돌 가능성이 있는 인터페이스 변경이 있었는지 확인한다.
- 필요한 경우 docs/rw/충돌 이슈.md에 기록한다.

### 섹션 9 검토 기준

- 섹션 8 구현 결과가 다른 모듈과 연동 가능한 형태인지 확인한다.
- API 계약, JSON 필드명, 상태값, 파일 경계가 문서와 일치하는지 확인한다.

### 섹션 11 검토 기준

- 섹션 10 구현 결과를 최종 구현 전 단계 수준으로 검토한다.
- 섹션 12 최종 심층검증에 들어가기 전에 남은 문제를 정리한다.
- 미해결 이슈가 있으면 명확히 표시한다.

---

## 7. 초기 상태

```txt
문서 생성 상태: 초기 템플릿 생성 완료 후 섹션 3부터 사용 시작
등록된 디버그 기록: DEBUG-심현석-0001 ~ DEBUG-심현석-0003
최근 사용 시점: 심현석의 에이전트가 섹션 7 검토/패치 단계 완료한 시점
```
