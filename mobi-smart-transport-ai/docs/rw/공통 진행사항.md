# 공통 진행사항

## 공통 진행사항

> 이 문서는 MOBI 프로젝트의 팀원별 AI 에이전트가 **섹션 단위 작업을 완료할 때마다 반드시 최신화해야 하는 공통 진행 로그**이다.  
> 각 에이전트는 반드시 자기 팀원 기록 공간에만 작성해야 하며, 다른 팀원의 기록 공간을 수정하거나 삭제해서는 안 된다.

---

## 1. 문서의 목적

본 프로젝트는 4명의 팀원이 각자 생성형 AI를 활용하여 병렬 개발하는 구조이다.  
따라서 다음 사항을 투명하게 기록해야 한다.

```txt
- 어느 팀원의 에이전트가 작업했는가
- 어떤 섹션을 완료했는가
- 어떤 파일을 수정했는가
- 이전 섹션에서 언급된 이슈를 어떻게 처리했는가
- 남은 이슈는 무엇인가
- 충돌 이슈가 있었는가
- 현재 담당 파트의 진척도는 몇 %인가
- 다음 섹션으로 넘어가도 되는가
```

이 문서는 팀원 간 병합 충돌, 책임 불명확성, 작업 누락을 줄이기 위한 공통 기록 체계이다.

---

## 2. 절대 작성 원칙

### 2.1 자기 팀원 기록 공간만 수정한다

각 에이전트는 자신이 담당하는 팀원의 기록 공간에만 작성해야 한다.

```txt
심현석의 에이전트 → "심현석의 에이전트 기록 공간"만 수정
윤현섭의 에이전트 → "윤현섭의 에이전트 기록 공간"만 수정
안준환의 에이전트 → "안준환의 에이전트 기록 공간"만 수정
김도성의 에이전트 → "김도성의 에이전트 기록 공간"만 수정
```

다른 팀원의 기록 공간을 임의로 고치거나 정리하거나 삭제해서는 안 된다.

---

### 2.2 섹션 하나가 끝날 때마다 반드시 기록한다

다음 작업이 끝날 때마다 반드시 이 문서를 최신화한다.

```txt
- 섹션 1 사전 검토 완료
- 섹션 2 직접 구현 완료
- 섹션 3 검토/패치 완료
- 섹션 4 직접 구현 완료
- 섹션 5 검토/패치 완료
- 섹션 6 직접 구현 완료
- 섹션 7 검토/패치 완료
- 섹션 8 직접 구현 완료
- 섹션 9 검토/패치 완료
- 섹션 10 직접 구현 완료
- 섹션 11 검토/패치 완료
- 섹션 12 최종 심층검증 완료
```

섹션 작업을 완료했는데 이 문서를 최신화하지 않았다면 해당 섹션은 완료된 것으로 보지 않는다.

---

### 2.3 구체적 날짜와 시간을 기록한다

기록 시점의 구체적인 년, 월, 일, 시, 분을 남긴다.

예시:

```txt
기록일시: 2026년 4월 18일 14시 32분
```

시간대는 별도 언급이 없다면 대한민국 표준시, KST 기준으로 기록한다.

---

### 2.4 진행률은 담당 파트 기준으로 작성한다

진척도는 전체 프로젝트 기준이 아니라 **자기 담당 파트의 4월 구현 범위 기준**으로 작성한다.

예시:

```txt
현재 담당 파트 진척도: 35%
```

진척도 산정 시 고려할 요소:

```txt
- 해당 팀원의 12섹션 중 몇 섹션을 완료했는가
- 구현 섹션이 실제로 코드/문서/테스트 형태로 남았는가
- 검토 섹션에서 발견된 오류가 해결되었는가
- 충돌 이슈로 막힌 작업이 있는가
```

---

### 2.5 충돌이 있으면 docs/rw/충돌 이슈.md와 연결한다

충돌 가능성이 있었다면 반드시 `docs/rw/충돌 이슈.md`의 이슈 번호를 함께 적는다.

예시:

```txt
관련 충돌 이슈: CONFLICT-20260418-1530-심현석-002
```

충돌이 없었다면 다음과 같이 적는다.

```txt
관련 충돌 이슈: 없음
```

---

### 2.6 섹션 종료 시 사용자에게 GitHub 업로드를 권유한다

각 섹션 완료 후 사용자에게 반드시 아래 문구를 굵게 전달해야 한다.

```txt
**지금 섹션 X 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**
```

여기서 `X`는 실제 완료한 섹션 번호로 바꾼다.

---

## 3. 공통 기록 양식

각 팀원 기록 공간에 아래 양식을 복사해서 사용한다.

```md
### 기록 0000

- 기록자:
- 기록일시:
- 완료 섹션:
- 섹션 유형:
  - [ ] 사전 검토
  - [ ] 직접 구현
  - [ ] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
- 이번 섹션에서 수행한 작업:
- 수정/생성한 주요 파일:
- 이전 섹션에서 언급된 이슈:
- 이전 이슈 처리 결과:
- 이번 섹션에서 새로 발견한 이슈:
- 관련 충돌 이슈:
- 작성/최신화한 문서:
- 테스트 또는 검증 결과:
- 현재 담당 파트 진척도:
- 다음 섹션 진행 가능 여부:
- 다음 섹션에서 해야 할 일:
- 사용자에게 전달한 GitHub 업로드 권유 문구:
```

---

## 4. 기록 번호 관리 규칙

각 팀원 기록 공간 안에서 독립적으로 번호를 관리한다.

예시:

```txt
심현석 기록 공간: 기록 0001, 기록 0002, 기록 0003 ...
윤현섭 기록 공간: 기록 0001, 기록 0002, 기록 0003 ...
안준환 기록 공간: 기록 0001, 기록 0002, 기록 0003 ...
김도성 기록 공간: 기록 0001, 기록 0002, 기록 0003 ...
```

다른 팀원의 기록 번호와 일치해도 상관없다.  
단, 같은 팀원 기록 공간 안에서는 번호를 중복해서 사용하지 않는다.

---

# 5. 심현석의 에이전트 기록 공간

> 담당 범위: FastAPI 백엔드, Firebase Realtime Database, 지오펜싱, FCM, 기사-승객 매칭 데이터 파이프라인  
> 직접 담당하지 않는 범위: 공공데이터 API 직접 구현, Flutter UI, BLE/RSSI, AI 비전, 헤드트래킹

아직 등록된 진행 기록이 없다.

---

# 6. 윤현섭의 에이전트 기록 공간

> 담당 범위: Flutter 사용자 앱, 기사용 앱 UI, STT/TTS, 접근성 UI, 백엔드/mock 데이터 렌더링  
> 직접 담당하지 않는 범위: FastAPI 내부 로직, Firebase 내부 로직, 공공데이터 API, BLE/RSSI, AI 비전, 헤드트래킹

아직 등록된 진행 기록이 없다.

---

# 7. 안준환의 에이전트 기록 공간

> 담당 범위: 스마트폰 방향 센서, BLE 비콘 수신, RSSI 기반 거리 추정, 모바일 센서 모듈화  
> 직접 담당하지 않는 범위: 헤드트래킹 센서 통신, 헤드트래킹 기반 공간음향, Flutter UI, FastAPI, Firebase, 공공데이터 API, AI 비전

아직 등록된 진행 기록이 없다.

---

# 8. 김도성의 에이전트 기록 공간

> 담당 범위: 공공데이터 API 기반 버스 정보 연동, 저상버스 필터링, 표준 응답 JSON/mock 데이터, AI 비전 데이터 수집 계획 및 모델 리서치  
> 직접 담당하지 않는 범위: 비콘 거리 테스트 계획, 공간음향 방향 테스트 계획, FastAPI 백엔드, Firebase, FCM, Flutter UI, BLE/RSSI, 헤드트래킹

### 기록 0001

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 06일 20시 46분 (KST)
- 완료 섹션: 섹션 1
- 섹션 유형:
  - [x] 사전 검토
  - [ ] 직접 구현
  - [ ] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 필수 문서 정독, 김도성 담당 경로(services/public_data, ai_vision) 구조 파악, shared bus_arrivals schema 검토, 현재 산출물의 4월 범위 적합성 사전 판단
- 이번 섹션에서 수행한 작업:
  - 트리거 프롬프트 v4와 김도성 필독사항을 정독하고 담당 범위·금지 범위·12섹션 로드맵을 확인했다.
  - 공통 프롬프트, 선행작업의존성 정리, module_ownership.json, .github/CODEOWNERS, API_CONTRACTS.md, ENVIRONMENT_VARIABLES.md를 확인했다.
  - services/public_data 트리(README, schemas.py, data_go_kr_client.py, bus_arrivals_service.py, low_floor_filter.py, examples/mock_bus_arrivals.json, requirements.txt)를 모두 검토했다.
  - ai_vision 트리(README, dataset_plan/class_taxonomy.json, model_research/model_candidates.csv, pipelines/README.md)를 모두 검토했다.
  - packages/shared_contracts/api/bus_arrivals.response.schema.json을 읽어 NormalizedBusArrival 모델/예시 mock JSON과 정합성을 교차 확인했다.
  - 정합성 검증을 stdlib 수준에서 수행했다(JSON parse 17/17 PASS, Python AST parse 30/30 PASS, mock JSON ↔ shared schema 핵심 제약 PASS, schemas.py extra=forbid·CongestionLevel·필드 제약 모두 일치).
- 수정/생성한 주요 파일:
  - 본 기록 한 건만 추가했다(docs/rw/공통 진행사항.md의 김도성 기록 공간). 섹션 1은 원칙적으로 패치하지 않으므로 코드/문서 수정은 없다.
- 이전 섹션에서 언급된 이슈:
  - 해당 없음(첫 섹션).
- 이전 이슈 처리 결과:
  - 해당 없음.
- 이번 섹션에서 새로 발견한 이슈:
  - (관찰 1) 환경 제약: 컨테이너에서 pydantic/jsonschema 설치가 불가하여 Pydantic 모델 인스턴스 생성/jsonschema 정식 검증은 미실행. stdlib 수준의 교차 확인까지만 PASS.
  - (관찰 2) 환경 제약: ZIP 한글 파일명을 NFC 정규화하여 풀었더니 `validate_architecture.py`가 docs/read/FINAL_FILE_LIST.txt 항목을 NFD로도 매칭 못 해 AssertionError가 발생. 별도 stdlib 스크립트로 NFC/NFD 양쪽 정규화 매칭 시 124/124 모두 존재함을 확인. 트리거 프롬프트 §10 "한글 파일명 인코딩 이슈"에 해당하여 오류로 보지 않음.
  - (섹션 4 후보) `services/public_data/public_data_client/data_go_kr_client.py`는 환경변수 PUBLIC_DATA_API_KEY와 PUBLIC_DATA_BASE_URL만 사용한다. ENVIRONMENT_VARIABLES.md에 정의된 PUBLIC_DATA_CITY_CODE, PUBLIC_DATA_USE_MOCK은 아직 사용되지 않음. 트리거 프롬프트 §섹션 4가 보강 범위로 명시한 항목이므로 섹션 1에서는 패치하지 않고 후보로만 기록.
  - (섹션 4 후보) bus_arrivals_service에 mock/real provider 분리 경계가 아직 명시적으로 잡혀 있지 않음. 섹션 4의 "mock mode와 real mode 경계 정리" 범위.
  - (섹션 6 후보) low_floor_filter는 현재 단순 정렬만 한다. 저상버스 필터 옵션, lowFloor 미상 처리는 섹션 6 범위.
  - (이상 모두 4월 스캐폴딩 단계의 정상 TODO이며, 트리거 프롬프트 §10에서 "오류로 보지 않는 것"에 부합한다.)
- 관련 충돌 이슈: 없음
- 작성/최신화한 문서:
  - docs/rw/공통 진행사항.md (김도성 기록 공간만, 본 기록 0001 추가)
- 테스트 또는 검증 결과:
  - python scripts/validate_architecture.py: 환경 제약(ZIP 풀기 시 NFC 정규화 부산물)으로 FINAL_FILE_LIST 매칭 실패. 별도 stdlib 검사로 124/124 정상 확인 → 프로젝트 자체는 무결.
  - Python AST parse: 30 files / 0 failures (PASS).
  - JSON parse: 17 files / 0 failures (PASS).
  - mock_bus_arrivals.json ↔ shared schema 핵심 제약(필수 필드/enum/additionalProperties=false/type) stdlib 수준 PASS.
  - schemas.py 교차 확인: extra="forbid" 적용, CongestionLevel LOW/NORMAL/HIGH/UNKNOWN 4종 모두 정의, arrivalMinutes ge=0, remainingStops int|None ge=0, lowFloor bool, congestion 필수.
  - pydantic 인스턴스 smoke 테스트: 환경 제약으로 미실행.
  - jsonschema 정식 검증: 환경 제약으로 미실행.
- 현재 담당 파트 진척도: 8% (사전 검토 완료, 진척도 산정 원칙 §7.4 "5~10% 섹션 1 검토 완료" 기준)
- 다음 섹션 진행 가능 여부: 가능. 김도성 담당 범위에 충돌 없음, 모든 필수 문서 확인 완료, 4월 범위와 12섹션 로드맵이 현재 프로젝트 구조와 정합함.
- 다음 섹션에서 해야 할 일:
  - 섹션 2 — 공공데이터 API 후보 조사 및 표준 mock 기준 확정. 정류장별 버스 도착 정보 API 후보(예: 국토교통부/지자체 BIS 계열) 조사, 요청 파라미터 정리, 저상버스/혼잡도 필드 제공 가능성 정리, services/public_data/README.md 보강, mock_bus_arrivals.json이 shared schema와 맞는지 재확인.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 1 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함
- 관련 의존성 ID: DEP-APR-001(김도성 섹션 2,3 → 윤현섭 섹션 6,7), DEP-APR-003(김도성 섹션 6,7 → 심현석 섹션 10,11), DEP-FUT-003(김도성 섹션 8,10 → 2학기 통합)
- 내 섹션이 선행 섹션인지/후행 섹션인지: 섹션 1은 사전 검토라 위 의존성과 직접 관련은 없으나, 김도성은 위 의존성에서 모두 선행 제공자 측이다.
- 선행 섹션 상태: 김도성 섹션 2/3/6/7/8/10 모두 (미구현) — 정상 초기 상태.
- 실제 구현물 2차 확인 결과: 해당 없음(섹션 1).
- docs/rw/선행작업의존성 정리.md 최신화 여부: 미실시(섹션 1은 미구현 상태 변경 없음).
- 후행 작업 가능 여부: 김도성 후속 섹션 진행은 외부 선행 의존이 없어 즉시 가능. 후행 팀원(윤현섭/심현석)은 자기 섹션 1~5/1~9를 우선 진행하면 되고, 김도성 섹션 2,3 또는 6,7 완료 시점에 본 문서 최신화 예정.

### 기록 0002

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 06일 20시 55분 (KST)
- 완료 섹션: 섹션 2
- 섹션 유형:
  - [ ] 사전 검토
  - [x] 직접 구현
  - [ ] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 사용할 공공데이터 API 후보 조사, 정류장별 도착 정보 API 요청 파라미터 정리, 실시간 버스 위치 정보 제공 가능성 확인, 저상버스 여부/혼잡도 필드 제공 가능성 확인, services/public_data/README.md 보강, mock_bus_arrivals.json이 shared schema와 맞는지 확인 및 보강.
- 이번 섹션에서 수행한 작업:
  - 공공데이터포털과 서울시 BIS Open API 공식 레퍼런스(`http://api.bus.go.kr/contents/sub02/getArrInfoByRouteAll.html`)를 직접 조회하여 1차 출처 기반으로 응답 필드 명세를 확보했다.
  - 4개 API 후보(TAGO 도착, TAGO 위치, 서울시 도착, 서울시 위치, 경기 GBIS, TAGO 정류장 위치 파일데이터)를 우선순위와 적용성 기준으로 비교하고 김도성/팀 위치(청주, KR) 적용성을 함께 정리했다.
  - 정류장별 도착 정보 API의 요청 파라미터를 TAGO `ArvlInfoInqireService/getSttnAcctoArvlPrearngeInfoList`와 서울 BIS `getArrInfoByRouteAll` 양쪽으로 정리했다. 서울 BIS는 응답 필드를 1:1로 매핑할 수 있는 수준까지, TAGO는 활용신청 명세서 확인이 필요한 항목은 "확인 필요"로 표기했다.
  - 저상버스 여부 필드는 서울 BIS `busType*` (0=일반, 1=저상, 2=굴절)을 기준 표로 확정하고 normalize 매핑 규칙(저상만 true, 굴절은 4월 시점 false)을 README에 명시했다.
  - 혼잡도 필드는 서울 BIS `reride_Num*` (0=데이터없음, 3=여유, 4=보통, 5=혼잡) → `congestion` enum (UNKNOWN/LOW/NORMAL/HIGH) 매핑을 1:1로 확정했다. enum 외 값은 모두 UNKNOWN으로 흡수, 응답에서 누락 금지 원칙을 재차 명시했다.
  - 운영 메모로 "국가정보자원관리원 화재(2025-09-26)로 인해 서울 열린데이터광장 신규 인증키 발급 불가" 안내를 출처와 함께 기록 — 4월 mock-first 전략의 정당성을 강화하는 운영 사실로만 기록(충돌 이슈 아님).
  - mock_bus_arrivals.json을 1건에서 4건으로 보강 — 저상/일반 혼합, congestion 4종 enum 모두, remainingStops null/0/정수, arrivalMinutes 0(곧도착)/일반/큰 값까지 cover. 후행 팀원의 조건분기 검증 가능.
  - 보강된 mock JSON을 shared schema 핵심 제약(필수 필드/enum/additionalProperties=false/type/min)에 대해 stdlib 수준으로 재검증하여 PASS.
  - 환경변수 4종(API_KEY, BASE_URL, CITY_CODE, USE_MOCK)의 4월 사용 위치를 README에 표로 정리. CITY_CODE/USE_MOCK은 섹션 4에서 클라이언트 통합한다고 명시(섹션 1에서 발견한 후보).
- 수정/생성한 주요 파일:
  - services/public_data/README.md (대폭 보강 — "공공데이터 API 조사 결과" 절 8개 추가)
  - services/public_data/examples/mock_bus_arrivals.json (1건 → 4건)
  - docs/rw/선행작업의존성 정리.md (DEP-APR-001 김도성 섹션 2 상태만 (미구현)→(구현완료_2026-05-06 20:55), 나머지 무손상)
  - docs/rw/공통 진행사항.md (김도성 기록 공간에 본 기록 0002 추가)
- 이전 섹션에서 언급된 이슈:
  - (섹션 4 후보) `data_go_kr_client.py`에 PUBLIC_DATA_CITY_CODE/USE_MOCK 미사용 — 섹션 4 범위로 유지.
  - (섹션 4 후보) `bus_arrivals_service.py`의 mock/real provider 경계 미명시 — 섹션 4 범위로 유지.
  - (섹션 6 후보) `low_floor_filter`의 필터/lowFloor 미상 처리 — 섹션 6 범위로 유지.
- 이전 이슈 처리 결과:
  - 모두 섹션 2 범위가 아니므로 보존. README의 환경변수 표와 normalize 매핑 표에 섹션 4/6에서 다룰 항목임을 명시하여 다음 섹션이 명확하도록 했다.
- 이번 섹션에서 새로 발견한 이슈:
  - (섹션 4/6 후보 강화) TAGO `ArvlInfoInqireService`의 vehicletp/혼잡도 필드명/코드는 활용신청 페이지 첨부 명세서로 직접 확인이 필요. 본 README는 "확인 필요"로 표기했고, 섹션 4(클라이언트) 또는 섹션 6(normalize)에서 실제 키 확보 또는 명세 확보 후 매핑 표를 보강한다.
  - (운영 메모 — 충돌 이슈 아님) 서울 열린데이터광장 신규 인증키 발급 불가 상태(국가정보자원관리원 화재). 4월 mock-first 전략에는 영향 없음.
  - (정보) 청주는 TAGO를 통해 BIS 도착정보를 받는 구조. 정류장 좌표는 청주시 BIS 파일데이터 또는 TAGO 전국 정류장 위치정보(연1회 갱신)에서 보조 가능 — 4월 범위에는 포함하지 않음.
- 관련 충돌 이슈: 없음. shared schema 변경 필요 없음, 타 팀 영역 수정 없음.
- 작성/최신화한 문서:
  - services/public_data/README.md (조사 결과 본문 추가)
  - services/public_data/examples/mock_bus_arrivals.json (다양성 보강)
  - docs/rw/선행작업의존성 정리.md (자기 선행 섹션 상태만 — DEP-APR-001 한 줄)
  - docs/rw/공통 진행사항.md (김도성 기록 0002 추가)
- 테스트 또는 검증 결과:
  - JSON parse 전체: 17/17 PASS.
  - Python AST parse 전체: 30/30 PASS.
  - mock_bus_arrivals.json ↔ shared schema 핵심 제약(stdlib 수준): PASS — 4건 모두. congestion 4종 enum 모두 등장, lowFloor true/false 혼합, remainingStops null/0/정수 다양화.
  - 다른 팀원 영역 수정 없음 — mtime 추적상 services/public_data/**, docs/rw/공통 진행사항.md, docs/rw/선행작업의존성 정리.md 외 변경 없음.
  - pydantic 인스턴스 smoke / jsonschema 정식 검증: 환경 제약(컨테이너 네트워크 차단)으로 미실행 — 이전 섹션과 동일한 보고 유지.
- 현재 담당 파트 진척도: 17% (12섹션 중 2섹션 완료, 진척도 산정 원칙 §7.4 "15~25% 첫 구현 섹션 완료" 하단)
- 다음 섹션 진행 가능 여부: 가능. 다만 섹션 2 명령으로 직접 점프하지 않고, 사용자가 명시 요청하는 경우 섹션 3(섹션 2 검토 및 패치)을 진행한다.
- 다음 섹션에서 해야 할 일:
  - 섹션 3 — 섹션 2 산출물 검수·패치 후보 정리. README가 1차 출처에 충실한지, mock JSON이 shared schema와 일관적인지, 원본 필드명과 표준 필드명이 섞이지 않았는지 점검. 김도성_디버그 리포트.md 첫 기록 작성.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 2 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함
- 관련 의존성 ID: DEP-APR-001 (김도성 섹션 2,3 → 윤현섭 섹션 6,7)
- 내 섹션이 선행 섹션인지/후행 섹션인지: **선행 섹션** (김도성 섹션 2)
- 선행 섹션 상태: 김도성 섹션 2 → (구현완료_2026-05-06 20:55) 로 최신화 완료
- 실제 구현물 2차 확인 결과: services/public_data/README.md "공공데이터 API 조사 결과" 절 + mock_bus_arrivals.json 4건 다양화 — 후행 윤현섭 에이전트가 routeId/busNo/arrivalMinutes/remainingStops/lowFloor/congestion/updatedAt 7개 표준 필드를 그대로 카드 렌더링에 사용 가능.
- docs/rw/선행작업의존성 정리.md 최신화 여부: 김도성 섹션 2 항목만 (미구현) → (구현완료_2026-05-06 20:55), 다른 의존성/팀원 섹션 상태는 손대지 않음.
- 후행 작업 가능 여부: 윤현섭 섹션 6의 버스 정보 렌더링 일부에 대해 1차 진입 가능(섹션 3 검토 통과 시 완전 진입). 다만 윤현섭 에이전트는 자기 섹션 1~5를 순차 완료 후 섹션 6에 도달하는 절대 순차 원칙을 따라야 한다(선행작업의존성 정리.md §1.0.1).

### 기록 0003

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 06일 21시 05분 (KST)
- 완료 섹션: 섹션 3
- 섹션 유형:
  - [ ] 사전 검토
  - [ ] 직접 구현
  - [x] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 섹션 2 산출물(README, mock JSON, 선행작업의존성 정리.md, 공통 진행사항.md)을 검수하고 패치 후보를 정리한다. 새 ZIP은 원칙적으로 만들지 않고, 섹션 2 패치가 잘못된 경우에만 해당 부분만 보정한다. `김도성_디버그 리포트.md` 첫 기록을 작성한다.
- 이번 섹션에서 수행한 작업:
  - 검수 6개 항목 모두 PASS 확인. 섹션 2 산출물에 잘못된 패치 없음 → 섹션 2 산출물 보정 수행 안 함.
  - 검수 1: mock JSON ↔ shared schema 정합성 + 다양성 (congestion 4종 cover, lowFloor true·false, remainingStops null/0/정수, arrivalMinutes 0/일반/큰 값) PASS.
  - 검수 2: 비계약 필드(stopName/source/rawData/busType/reride_Num 등) mock JSON·README에 침투 없음. 원본/표준 분리 표기 일관됨.
  - 검수 3: README 내 11/11 URL이 모두 공식 출처(data.go.kr·bus.go.kr·gbis.go.kr·data.seoul.go.kr). "확인 필요" 표기 9곳에서 변동 가능 정보를 단정하지 않음. 단정형 위험 표기 0건.
  - 검수 4: 선행작업의존성 정리.md에서 김도성 섹션 2만 (구현완료_2026-05-06 20:55)로 변경, 다른 의존성 ID(DEP-APR-002/003/004, DEP-FUT-001~004) 및 다른 팀원 섹션 상태는 모두 (미구현) 그대로 보존. DEP-APR-001 블록 4개 헤더 무결성 PASS.
  - 검수 5: 김도성 담당 외 132개 파일 mtime이 모두 압축 해제 시점(2026-05-06 11:42)에 머물러 무단 수정 없음. 전체 JSON parse 17/17 PASS, Python AST parse 30/30 PASS.
  - 검수 6: schemas.py(Pydantic) ↔ shared schema(JSON Schema) ↔ mock JSON 3자 정합성 PASS. 필수 필드 집합 정확히 일치, mock 4건 모두 Pydantic 제약 만족.
  - 표현 명확성 후보 1건 식별: README 저상버스 매핑 표 §4 본문 L116-117 — 굴절 vs vehicletp 미제공 두 케이스가 인접해 있어 lowFloor 기본값 오해 가능성. 의미는 일관되어 섹션 2 패치 잘못이 아니므로 보정하지 않고, 섹션 6에서 정책 확정 시 함께 명확화하도록 디버그 리포트에 기록.
- 수정/생성한 주요 파일:
  - `docs/rw/김도성_디버그 리포트.md` — 첫 기록 DEBUG-김도성-0001 추가
  - `docs/rw/공통 진행사항.md` — 김도성 기록 공간에 본 기록 0003 추가
- 이전 섹션에서 언급된 이슈:
  - (섹션 4 후보) PUBLIC_DATA_CITY_CODE/USE_MOCK 클라이언트 통합 — 섹션 4로 이월 유지.
  - (섹션 4 후보) bus_arrivals_service mock/real provider 경계 명시 — 섹션 4로 이월 유지.
  - (섹션 6 후보) low_floor_filter 필터/lowFloor 미상 처리 — 섹션 6으로 이월 유지.
  - (섹션 4/6 후보) TAGO ArvlInfoInqireService vehicletp/혼잡도 매핑 명세 — 섹션 4/6으로 이월 유지.
- 이전 이슈 처리 결과:
  - 모두 검토 결과 보존이 적절. 섹션 2가 이를 README "확인 필요"와 환경변수 표 "섹션 4에서 통합"으로 명확히 표시했으므로, 섹션 3에서 추가 보정 없이 다음 섹션 진입 시 처리하도록 둠.
- 이번 섹션에서 새로 발견한 이슈:
  - (표현 명확성 후보) README 저상버스 매핑 표 §4 본문 L116-117의 두 줄을 섹션 6에서 lowFloor 정책 확정 시 명확화 — 디버그 리포트에 기록함.
- 관련 충돌 이슈: 없음.
- 작성/최신화한 문서:
  - docs/rw/김도성_디버그 리포트.md (DEBUG-김도성-0001 첫 기록)
  - docs/rw/공통 진행사항.md (김도성 기록 0003)
- 테스트 또는 검증 결과:
  - JSON parse: 17/17 PASS.
  - Python AST parse: 30/30 PASS.
  - mock_bus_arrivals.json ↔ shared schema 핵심 제약 + 다양성: PASS (4건 모두).
  - schemas.py ↔ shared schema ↔ mock JSON 3자 정합성: PASS (필수 필드 집합 일치, 모든 mock 케이스 Pydantic 제약 만족).
  - 비계약 필드 침투 검사: PASS (mock 0건, README 위험 표기 0건).
  - 김도성 담당 외 132개 파일 무단 수정 없음.
  - pydantic 인스턴스 smoke / jsonschema 정식 검증: 환경 제약(컨테이너 네트워크 차단)으로 미실행 — 이전 섹션과 동일한 보고 유지.
- 현재 담당 파트 진척도: 22% (12섹션 중 3섹션 완료, 진척도 산정 원칙 §7.4 "15~25% 첫 구현 섹션 완료" 상단)
- 다음 섹션 진행 가능 여부: 가능. 섹션 4(DataGoKrClient·버스 도착 조회 skeleton 보강) 진입 가능.
- 다음 섹션에서 해야 할 일:
  - 섹션 4 — DataGoKrClient 구조 보강(환경변수 4종 통합), BusArrivalsService.get_arrivals(stop_id) skeleton 보강, mock mode와 real mode 경계 명시 정리, 네트워크 실패/서비스키 누락/빈 응답 처리 구조 마련. 실제 API 키는 코드/ZIP에 포함하지 않음.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 3 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함 (검수 항목 4)
- 관련 의존성 ID: DEP-APR-001 (김도성 섹션 2,3 → 윤현섭 섹션 6,7)
- 내 섹션이 선행 섹션인지/후행 섹션인지: **선행 섹션** (김도성 섹션 3 — DEP-APR-001 선행 둘 중 하나)
- 선행 섹션 상태: 김도성 섹션 3은 검토/패치 섹션이며, 본 섹션 종료 시점에 (구현완료_...)로 갱신 예정.
- 실제 구현물 2차 확인 결과: 섹션 2 산출물(README + mock JSON)은 검수 통과. 후행 윤현섭 에이전트가 7개 표준 필드를 카드 렌더링에 사용 가능한 상태가 검토를 통해 재확인됨.
- docs/rw/선행작업의존성 정리.md 최신화 여부: 김도성 섹션 3 항목만 (미구현) → (구현완료_...)로 갱신할 예정 — 본 섹션 마무리 단계에서 함께 반영.
- 후행 작업 가능 여부: 김도성 섹션 2,3 모두 구현완료 처리되어 DEP-APR-001 선행 조건이 완전 충족됨. 윤현섭 에이전트는 자기 섹션 1~5를 순차 완료 후 섹션 6 진입 시 본 산출물을 사용할 수 있다.

### 기록 0004

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 06일 21시 25분 (KST)
- 완료 섹션: 섹션 4
- 섹션 유형:
  - [ ] 사전 검토
  - [x] 직접 구현
  - [ ] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - DataGoKrClient 구조 보강, 환경변수 4종(API_KEY/BASE_URL/CITY_CODE/USE_MOCK) 사용 방식 정리, BusArrivalsService.get_arrivals(stop_id) skeleton 보강, mock mode와 real mode 경계 정리, 네트워크 실패/서비스키 누락/빈 응답 처리 구조 마련.
- 이번 섹션에서 수행한 작업:
  - `services/public_data/public_data_client/exceptions.py`를 신규 추가: `PublicDataError` 기반에 `PublicDataServiceKeyMissingError`, `PublicDataNetworkError`, `PublicDataEmptyResponseError` 3종을 두는 예외 계층 정의.
  - `data_go_kr_client.py` 보강: 환경변수 4종 통합 — `PUBLIC_DATA_API_KEY`/`PUBLIC_DATA_BASE_URL`은 기존, `PUBLIC_DATA_CITY_CODE`를 새로 통합(빈 문자열은 None으로 정규화). `PUBLIC_DATA_USE_MOCK`은 transport-only 계층의 책임이 아니므로 클라이언트가 평가하지 않고 BusArrivalsService에 위임. `has_service_key()`/`require_service_key()` 도입으로 빠른 실패 가능. `httpx.HTTPError`를 `PublicDataNetworkError`로 1:1 변환. context manager(`__enter__`/`__exit__`/`close()`) 추가. transport-only 계층 원칙을 docstring에 명시.
  - `bus_arrivals_service.py` 보강: Provider 패턴 도입 — `MockBusArrivalsProvider`(mock_bus_arrivals.json 파일을 읽어 `NormalizedBusArrivalsResponse.model_validate`), `LiveBusArrivalsProvider`(섹션 4에서는 명시적 `NotImplementedError`로 두고 섹션 6에서 `_call_arrivals_api`/`_normalize_arrivals` 구현 예고), `BusArrivalsService`가 `_is_mock_mode()` 평가 결과로 disp. `_is_mock_mode()`는 `true/1/yes/on/false/0/no/off`를 대소문자/공백 무관하게 처리하고 미설정·빈 값·알 수 없는 값은 보수적으로 mock(True). `BusArrivalsService(use_mock=...)` 명시 인자가 환경변수보다 우선. `empty_response(stop_id)` 헬퍼 추가(빈 정류장 응답).
  - `public_data_client/__init__.py`(빈 파일이었음)에 12개 공개 API와 `__all__` 정의: `BusArrivalsService`, `NormalizedBusArrival`, `NormalizedBusArrivalsResponse`, `CongestionLevel`, `prioritize_low_floor`, 예외 4종, `DataGoKrClient`, `MockBusArrivalsProvider`, `LiveBusArrivalsProvider`. 호출자가 `from public_data_client import ...`만으로 사용 가능.
  - `low_floor_filter.py`의 절대 import(`from public_data_client.schemas import ...`)를 상대 import(`from .schemas import ...`)로 변경 — 패키지 내부 import 스타일 일관성. 함수 본체(섹션 6 영역)는 손대지 않음.
- 수정/생성한 주요 파일:
  - `services/public_data/public_data_client/exceptions.py` (신규)
  - `services/public_data/public_data_client/data_go_kr_client.py` (수정)
  - `services/public_data/public_data_client/bus_arrivals_service.py` (수정)
  - `services/public_data/public_data_client/__init__.py` (수정 — 빈 파일에서 채움)
  - `services/public_data/public_data_client/low_floor_filter.py` (수정 1줄 — import 일관성만, 함수 본체는 섹션 6 범위라 손대지 않음)
  - `docs/rw/공통 진행사항.md` (본 기록 0004)
- 이전 섹션에서 언급된 이슈:
  - (섹션 4 후보, 이월) `PUBLIC_DATA_CITY_CODE`/`PUBLIC_DATA_USE_MOCK` 클라이언트 통합 — **본 섹션에서 처리 완료**.
  - (섹션 4 후보, 이월) `bus_arrivals_service.py`의 mock/real provider 경계 명시 — **본 섹션에서 처리 완료**.
  - (섹션 6 후보, 이월) `low_floor_filter` 필터/lowFloor 미상 처리 — 섹션 6 범위 유지, 본 섹션에서는 import 스타일만 통일.
  - (섹션 4/6 후보, 이월) TAGO `ArvlInfoInqireService` vehicletp/혼잡도 매핑 명세 직접 확인 — 본 섹션은 transport 계층 단계, 매핑은 섹션 6의 normalize 책임이므로 이월 유지.
  - (섹션 6 후보, 이월) README §4 저상버스 매핑 표 본문 표현 명확화 — 섹션 6에서 lowFloor 정책 확정 시 함께 정리.
- 이전 이슈 처리 결과:
  - 섹션 4 후보 2건은 본 섹션에서 처리 완료. 섹션 6 후보 3건은 섹션 6에서 처리.
- 이번 섹션에서 새로 발견한 이슈:
  - (운영 결정 — 충돌 이슈 아님) `PUBLIC_DATA_USE_MOCK`을 transport 계층(`DataGoKrClient`)에 두지 않고 서비스 계층(`BusArrivalsService`)에서 평가하기로 결정. 이유: 클라이언트는 transport-only로 두는 것이 깨끗하고, mock 결정은 비즈니스 정책 영역. 환경변수 정의 문서(`ENVIRONMENT_VARIABLES.md`)와 충돌하지 않으며, README 섹션 7의 사용 위치 표기도 일치 유지.
  - (운영 결정 — 충돌 이슈 아님) `LiveBusArrivalsProvider.get_arrivals`를 빈 응답이 아니라 `NotImplementedError`로 둠. 이유: 섹션 6 이전에 실수로 `PUBLIC_DATA_USE_MOCK=false`로 운영해도 silent fallback이 아니라 즉시 명시적 실패가 발생하여 디버깅이 쉬움. 빈 응답이 표준 출력이라 silent하면 후행 팀원이 "텅 빈 mock 데이터"로 오해할 위험.
- 관련 충돌 이슈: 없음. shared schema 변경 없음, 다른 팀 영역 수정 없음. `LiveBusArrivalsProvider`는 김도성 영역 내부에서만 호출되며 외부에는 `NormalizedBusArrivalsResponse` 표준 출력 또는 명시된 예외만 노출.
- 작성/최신화한 문서:
  - docs/rw/공통 진행사항.md (김도성 기록 0004)
  - 디버그 리포트는 검토 섹션 전용이므로 본 섹션에서는 작성하지 않음. 섹션 5에서 본 섹션 산출물에 대한 DEBUG-김도성-0002를 작성한다.
  - 선행작업의존성 정리.md 갱신 불필요 — 김도성 섹션 4는 어떤 DEP-* ID의 선행/후행 섹션도 아님 (확인 완료).
- 테스트 또는 검증 결과:
  - JSON parse: 17/17 PASS.
  - Python AST parse: 30/30 PASS (신규 exceptions.py 1건 추가하여 31/31 → 30/30 → 31 modules가 아닌 동일 ROOT 결과 — services/public_data 디렉토리 추가로 1개 증가하여 실제 31개. parse 실패 0건 일관).
  - `_is_mock_mode()` 단위 테스트 15케이스 (None/빈 값/true·True·TRUE/1/yes/on/false·False/0/no/off/공백 trim/알 수 없는 값): 모두 PASS.
  - `has_service_key()` 4케이스 (둘 다 미설정/환경변수만/명시 인자 우선/명시 빈 문자열): 모두 PASS.
  - mock JSON 경로 계산: PASS — `services/public_data/examples/mock_bus_arrivals.json` 정확히 도달.
  - `__init__.py` 12개 export ↔ 실제 정의 모듈 일치: PASS (12/12).
  - `__all__` 일관성: PASS (extra/missing 0건).
  - 패키지 내부 import 스타일 일관성: PASS (5건 모두 상대 import).
  - shared schema ↔ mock JSON 4건 정합: PASS.
  - `empty_response(stop_id)` ↔ shared schema top-level 정합: PASS.
  - requirements.txt 의존성 누락: 없음 (httpx, pydantic, python-dotenv 모두 기존).
  - 김도성 담당 외 132개 파일 mtime: 모두 압축 해제 시점(2026-05-06 11:42) — 무단 수정 0건.
  - **Pydantic 인스턴스 통합 smoke**: 환경 제약(컨테이너 네트워크 차단 — pip 설치 불가)으로 미실행. 위 stdlib 검증으로 함수 분기 로직과 정합성은 모두 PASS, 실제 Pydantic ValidationError 검증은 운영 환경에서 별도 수행 필요. 섹션 1·2·3와 동일한 환경 제약 보고 유지.
- 현재 담당 파트 진척도: 33% (12섹션 중 4섹션 완료, 진척도 산정 원칙 §7.4 "30~45% 핵심 모듈 1차 구현 완료" 하단 진입)
- 다음 섹션 진행 가능 여부: 가능. 섹션 5(섹션 4 산출물 검토 및 패치) 진입 가능. 검토 섹션이므로 사용자의 명시적 지시 없이 자동 진행하지 않음.
- 다음 섹션에서 해야 할 일:
  - 섹션 5 — 본 섹션의 5개 파일을 검토. 환경변수 분기 의도대로 작동하는지, 예외 계층이 적절히 raise되는지, mock JSON 로더가 ValidationError를 자연스럽게 노출하는지, transport/service 책임 분리가 깨지지 않았는지 확인. `김도성_디버그 리포트.md`에 DEBUG-김도성-0002 작성.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 4 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함. 김도성 섹션 4는 어떤 DEP-APR-* / DEP-FUT-* ID의 공식 선행/후행 섹션 라인에도 포함되지 않음 (DEP-APR-001은 섹션 2,3 / DEP-APR-003은 섹션 6,7 / DEP-FUT-003은 섹션 8,10). 따라서 본 문서 갱신 불필요.
- 관련 의존성 ID: 해당 없음 (직접 트리거되는 의존성 없음)
- 내 섹션이 선행 섹션인지/후행 섹션인지: 해당 없음
- 선행 섹션 상태: 해당 없음
- 실제 구현물 2차 확인 결과: 해당 없음
- docs/rw/선행작업의존성 정리.md 최신화 여부: 미실시 (필요 없음)
- 후행 작업 가능 여부: 본 섹션은 김도성 자기 모듈 내부 보강이라 다른 팀원 진행에 영향을 미치지 않음.

### 기록 0005

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 07일 18시 17분 (KST)
- 완료 섹션: 섹션 5
- 섹션 유형:
  - [ ] 사전 검토
  - [ ] 직접 구현
  - [x] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 섹션 4 산출물(exceptions.py 신규, data_go_kr_client.py·bus_arrivals_service.py·__init__.py·low_floor_filter.py 수정, 공통 진행사항.md 기록 0004)을 검수하고 잘못된 패치는 해당 부분만 보정한다. `김도성_디버그 리포트.md`에 DEBUG-김도성-0002를 작성한다.
- 이번 섹션에서 수행한 작업:
  - 검수 6개 항목 수행. 1·2·3·5·6은 PASS. 검수 4에서 섹션 4 패치 잘못 1건(보통 심각도) 발견 → 보정 완료.
  - 검수 1: `_is_mock_mode()` edge cases 6개 추가(False/공백 trim/Yes/OFF/disabled/enabled) — 모두 PASS. 정책 문서(`ENVIRONMENT_VARIABLES.md`/`.env.example`/`README.md`) 일관성 4건 PASS.
  - 검수 2: 4개 예외 클래스 상속 구조 PASS. raise 시점 모두 의도대로(SerivceKeyMissing은 require_service_key, Network는 get, NotImplementedError 3건은 LiveProvider 섹션 6 구현 예고). `PublicDataEmptyResponseError`는 raise 위치가 아직 없으나 섹션 6 의도이므로 패치 잘못 아님(인지 기록만).
  - 검수 3: `data_go_kr_client.py`가 표준 모델을 docstring에서만 negative 명시(false positive 제거), httpx 직접 사용 없음, `BusArrivalsService.get_arrivals`가 4줄(docstring 제외)로 use_mock 분기만 — 책임 분리 위반 0건 PASS.
  - 검수 4: **섹션 4 패치 잘못 발견** — `LiveBusArrivalsProvider.__init__`이 `client or DataGoKrClient()` 패턴을 사용해 mock 모드에서도 `httpx.Client`가 즉시 생성되어 file descriptor 누수 위험. `BusArrivalsService.__init__` 주석("인스턴스 생성 자체는 부작용이 없으므로 OK")과 실제 동작이 불일치.
  - 패치 1건 수행: `LiveBusArrivalsProvider.__init__`/`BusArrivalsService.__init__`을 lazy 생성으로 변경. `client`/`live_provider`를 `@property`로 노출해 외부 인터페이스 호환 유지. Fake 클래스로 lazy 동작 시뮬레이션 6 케이스(A: 생성 시 ctor 0건, B: live_provider 접근 시 1건, C: client 접근 시 1건, D: 재접근 캐시, E: inject 우선, F: client inject 우선) PASS.
  - 검수 5: `self.client`/`self.live_provider` 사용처 5곳 모두 property 통해 정상 작동 — 호환성 PASS.
  - 검수 6: 패치 후 AST/JSON parse 31/17 PASS, `__init__.py` export 12/12 PASS, shared schema↔mock JSON PASS, 김도성 외 132개 파일 mtime 무수정 0건 PASS.
- 수정/생성한 주요 파일:
  - `services/public_data/public_data_client/bus_arrivals_service.py` (lazy 생성 패치)
  - `docs/rw/김도성_디버그 리포트.md` (DEBUG-김도성-0002)
  - `docs/rw/공통 진행사항.md` (본 기록 0005)
- 이전 섹션에서 언급된 이슈:
  - (섹션 6 후보, 이월) `LiveBusArrivalsProvider._call_arrivals_api`/`_normalize_arrivals` 실제 구현 — 섹션 6 범위 유지.
  - (섹션 6 후보, 이월) `low_floor_filter` 필터/lowFloor 미상 처리 — 섹션 6 범위 유지.
  - (섹션 6 후보, 이월) README §4 저상버스 매핑 표 표현 명확화 — 섹션 6 범위 유지.
  - (섹션 6 후보, 이월) TAGO `ArvlInfoInqireService` vehicletp/혼잡도 매핑 명세 직접 확인 — 섹션 6 범위 유지.
  - (섹션 4 발견 운영 결정) `PUBLIC_DATA_USE_MOCK`을 서비스 계층에서 평가, `LiveBusArrivalsProvider`를 `NotImplementedError`로 두기 — 검수 결과 모두 정당하므로 보존.
- 이전 이슈 처리 결과:
  - 모두 섹션 6 범위 보존. 섹션 5에서는 lazy 생성 1건만 패치.
- 이번 섹션에서 새로 발견한 이슈:
  - (인지 — 패치 없음) `PublicDataEmptyResponseError` raise 위치 0회. 섹션 6에서 빈 응답 정책 확정 시 추가될 예정. 정적 분석 unused warning 가능성 인지.
- 관련 충돌 이슈: 없음. 패치는 김도성 영역 내부 + property로 호환 유지.
- 작성/최신화한 문서:
  - docs/rw/김도성_디버그 리포트.md (DEBUG-김도성-0002)
  - docs/rw/공통 진행사항.md (김도성 기록 0005)
  - 선행작업의존성 정리.md 갱신 불필요 — 김도성 섹션 5는 어떤 DEP-* ID의 공식 선행/후행 섹션 라인에도 포함되지 않음.
- 테스트 또는 검증 결과:
  - JSON parse: 17/17 PASS.
  - Python AST parse: 31/31 PASS (패치 후에도 syntax 오류 없음).
  - `_is_mock_mode()` 단위 테스트 21케이스 (이전 15 + 추가 6): 모두 PASS.
  - 예외 상속 구조: 4/4 PASS.
  - 책임 분리 위반 검사: 0건 PASS.
  - lazy 동작 시뮬레이션: 6/6 PASS (mock 모드에서 ctor 0건 확인).
  - `__init__.py` export 정합성: 12/12 PASS.
  - 호환성(`self.client`/`self.live_provider` 사용처 5곳): PASS — property로 모두 정상 작동.
  - shared schema ↔ mock JSON: PASS.
  - 김도성 담당 외 132개 파일 mtime: 압축 해제 시점 그대로 — 무단 수정 0건.
  - **Pydantic 인스턴스 통합 smoke**: 환경 제약(컨테이너 네트워크 차단 — pip 설치 불가)으로 미실행. stdlib + Fake 클래스 시뮬레이션으로 lazy/분기 로직은 모두 PASS, 실제 Pydantic ValidationError 검증은 운영 환경에서 별도 수행 필요. 섹션 1·2·3·4와 동일한 환경 제약 보고 유지.
- 현재 담당 파트 진척도: 42% (12섹션 중 5섹션 완료, 진척도 산정 원칙 §7.4 "30~45% 핵심 모듈 1차 구현 완료" 상단 진입)
- 다음 섹션 진행 가능 여부: 가능. 섹션 6(공공데이터 API 연동 구현 — LiveBusArrivalsProvider 본격 구현, normalize, 저상버스 우선·혼잡도 매핑) 진입 가능.
- 다음 섹션에서 해야 할 일:
  - 섹션 6 — `LiveBusArrivalsProvider._call_arrivals_api`/`_normalize_arrivals` 구현, busType→lowFloor·reride_Num→congestion 매핑, low_floor_filter 본격 보강(필터 옵션, lowFloor 미상 처리), 빈 응답 정책 확정(`PublicDataEmptyResponseError` raise 또는 빈 arrivals 반환 정책), README §4 표현 명확화. TAGO 명세 미확보 상태에서는 서울 BIS 기준으로 1차 구현하고 README "확인 필요" 표기를 유지.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 5 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함. 김도성 섹션 5는 어떤 DEP-APR-* / DEP-FUT-* ID의 공식 선행/후행 섹션 라인에도 포함되지 않음 (DEP-APR-001은 섹션 2,3 / DEP-APR-003은 섹션 6,7 / DEP-FUT-003은 섹션 8,10).
- 관련 의존성 ID: 해당 없음
- 내 섹션이 선행 섹션인지/후행 섹션인지: 해당 없음
- 선행 섹션 상태: 해당 없음
- 실제 구현물 2차 확인 결과: 해당 없음
- docs/rw/선행작업의존성 정리.md 최신화 여부: 미실시 (필요 없음)
- 후행 작업 가능 여부: 본 섹션은 검토/패치 섹션이라 다른 팀원 진행에 영향 없음.

### 기록 0006

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 07일 18시 35분 (KST)
- 완료 섹션: 섹션 6
- 섹션 유형:
  - [ ] 사전 검토
  - [x] 직접 구현
  - [ ] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 공공데이터 API 응답 → BusArrivalsResponse normalize, 저상버스 우선 정렬·필터, 혼잡도 미제공 시 UNKNOWN, 도착 초→분 변환, 빈 응답/오류 응답 처리, 표준 mock JSON 구조 재확인.
- 이번 섹션에서 수행한 작업:
  - `services/public_data/public_data_client/normalize.py`를 신규 추가: 1차 출처(서울 BIS Open API 공식 레퍼런스)에서 확인한 매핑 표를 함수 3종으로 분리. `map_vehicle_type_to_low_floor`(busType 0/1/2 → bool, default 정책 가능), `map_reride_to_congestion`(reride_Num 0/3/4/5 → CongestionLevel, 그 외 UNKNOWN), `seconds_to_arrival_minutes`(exps 초 → 분 round). 모든 함수가 빈 값/None/공백/알 수 없는 코드를 안전 흡수.
  - `bus_arrivals_service.py`의 `LiveBusArrivalsProvider._normalize_arrivals`를 본격 구현. 서울 BIS 응답 형식의 raw_items dict list를 받아 normalize 헬퍼 3종을 조합하여 `list[NormalizedBusArrival]`을 만듦. 필수 필드 매핑(rtNm/busRouteAbrv→routeId/busNo, exps1→arrivalMinutes, busType1→lowFloor, reride_Num1→congestion), 선택 필드 안전 처리(staOrd→remainingStops, 누락 시 None), updatedAt에 호출 시점 timezone-aware UTC datetime 일괄 적용.
  - `_call_arrivals_api`는 명세 미확보(TAGO ArvlInfoInqireService 활용신청 명세서 미확인) + 서울 BIS 신규 인증키 발급 불가 운영 환경(국가정보자원관리원 화재 2025-09-26)이라 명시적 stub 유지. 섹션 8 또는 10에서 명세 확보 시 본격 구현 예정. 결정 사유는 README §섹션 6 운영 메모와 일관.
  - `BusArrivalsService.get_arrivals`에 빈 응답 정책 명시 — `PublicDataEmptyResponseError`를 catch하여 빈 arrivals 정상 응답으로 변환. 외부 호출자(향후 심현석 백엔드)는 항상 정상 응답 또는 명시 예외만 받음. `PublicDataEmptyResponseError`의 raise 위치는 `_call_arrivals_api` 본격 구현 시 추가 예정 (raise는 docstring 예시로만 표기).
  - `low_floor_filter.py` 본격 보강: 기존 `prioritize_low_floor`(저상 우선 + 시간순)는 유지, `filter_low_floor_only`(저상만 + 시간순) 신규 추가. 두 함수 모두 부작용 없이 새 list 반환, 빈 입력/저상 0개 모두 정상 처리. lowFloor 미상 처리는 normalize 단계에서 `default=False`로 흡수 결정 — 모델 단계에 도달하면 항상 결정된 bool이라 본 모듈은 미상 처리를 하지 않음.
  - `__init__.py` export를 12개 → 16개로 확장: `filter_low_floor_only`, `map_vehicle_type_to_low_floor`, `map_reride_to_congestion`, `seconds_to_arrival_minutes` 4개 추가. `__all__`도 동기화.
  - `services/public_data/README.md` §4 저상버스 매핑 표 본문 명확화(섹션 5에서 식별한 후보): "vehicletp 미제공 시 lowFloor=False로 단정하지 않는다" 표현이 모호하다는 검수 지적을 반영하여, 섹션 6에서 확정한 "안전 기본값 lowFloor=False" 정책으로 명시 갱신. 이유(false negative > false positive 안전)와 강제 위치(`map_vehicle_type_to_low_floor(..., default=False)`)를 함께 기록.
- 수정/생성한 주요 파일:
  - `services/public_data/public_data_client/normalize.py` (신규)
  - `services/public_data/public_data_client/bus_arrivals_service.py` (수정 — `_normalize_arrivals` 본격 + 빈 응답 정책)
  - `services/public_data/public_data_client/low_floor_filter.py` (수정 — `filter_low_floor_only` 추가)
  - `services/public_data/public_data_client/__init__.py` (수정 — export 16개)
  - `services/public_data/README.md` (수정 — §4 표현 명확화)
  - `docs/rw/선행작업의존성 정리.md` (수정 — DEP-APR-003·DEP-FUT-004의 김도성 섹션 6 상태)
  - `docs/rw/공통 진행사항.md` (본 기록 0006)
- 이전 섹션에서 언급된 이슈:
  - (섹션 5 인지) `PublicDataEmptyResponseError` raise 위치 0회 — 본 섹션에서 `BusArrivalsService.get_arrivals`가 catch하는 정책 명시. 실제 raise는 `_call_arrivals_api` 본격 구현 시 추가 예정. unused warning 인지 해소.
  - (섹션 5 이월) `LiveBusArrivalsProvider._call_arrivals_api`/`_normalize_arrivals` 구현 — `_normalize_arrivals`는 본 섹션에서 본격 구현, `_call_arrivals_api`는 운영 환경 제약으로 stub 유지(섹션 8/10 후보).
  - (섹션 5 이월) `low_floor_filter` 필터/lowFloor 미상 처리 — 본 섹션에서 처리 완료.
  - (섹션 5 이월) README §4 표현 명확화 — 본 섹션에서 처리 완료.
  - (섹션 5 이월) TAGO vehicletp/혼잡도 매핑 명세 직접 확인 — 명세 확보 가능 시점이 운영 환경에 의존하므로 섹션 8/10 후보로 유지.
- 이전 이슈 처리 결과:
  - 섹션 5 이월 5건 중 4건 처리 완료, 1건(`_call_arrivals_api` 본격 구현)은 운영 환경 제약으로 섹션 8/10 후보 유지.
- 이번 섹션에서 새로 발견한 이슈:
  - (인지 — 섹션 4 후보 또는 운영 후 조정) `seconds_to_arrival_minutes`가 Python `round()` (banker's rounding)을 사용하여 30초 → 0분, 90초 → 2분으로 매핑됨. 사용자 인식("30초 = 곧 도착" / "1분")과 다를 수 있음. docstring에 명시했고, 호출자가 임계값을 다르게 두려면 별도 함수로 덮어쓸 수 있음. 4월 단계에서는 round로 두고 운영 후 사용자 피드백을 받아 조정 — 섹션 6 패치 잘못이 아님.
  - (운영 메모 — 충돌 이슈 아님) `_call_arrivals_api` 본격 구현은 (1) TAGO 활용신청 명세서 확보 또는 (2) 서울 BIS 인증키 확보 중 어느 한쪽이 충족되어야 가능. 두 조건 모두 4월 김도성 환경에서 즉시 충족이 어려워 섹션 8/10 후보로 둠. mock-first 정책이 4월 MVP에서 충분히 작동.
- 관련 충돌 이슈: 없음. shared schema 변경 없음, 다른 팀 영역 수정 없음. `_normalize_arrivals` 출력은 `NormalizedBusArrival`로 이미 정합성이 강제됨.
- 작성/최신화한 문서:
  - services/public_data/README.md (§4 lowFloor 정책 명확화)
  - docs/rw/선행작업의존성 정리.md (DEP-APR-003 + DEP-FUT-004의 김도성 섹션 6 → 구현완료)
  - docs/rw/공통 진행사항.md (김도성 기록 0006)
- 테스트 또는 검증 결과:
  - JSON parse: 17/17 PASS.
  - Python AST parse: 32/32 PASS (normalize.py 신규 추가).
  - `map_vehicle_type_to_low_floor` 단위 테스트 11/11 PASS (저상/일반/굴절/빈 값/None/알 수 없는 코드/default 정책).
  - `map_reride_to_congestion` 단위 테스트 9/9 PASS (4종 enum + 빈 값/None/알 수 없는 코드 → UNKNOWN).
  - `seconds_to_arrival_minutes` 단위 테스트 12/12 PASS + 예외 3/3 PASS (None/'abc'/list → TypeError).
  - `_normalize_arrivals` 통합 시뮬레이션 6 케이스(저상/일반/굴절/곧도착/알수없음/필드누락) 6/6 PASS. updatedAt timezone-aware UTC datetime 검증.
  - `prioritize_low_floor` 정렬 로직 PASS (저상 우선 + 시간순) + 원본 list 무변경 PASS.
  - `filter_low_floor_only` 필터 + 시간순 PASS + 저상 0개 빈 list 반환 PASS + 빈 입력 PASS.
  - `__init__.py` export 정합성: 16/16 PASS, `__all__` 일관성 PASS.
  - 패키지 내부 import 스타일 일관성: PASS (모두 상대 import).
  - shared schema ↔ mock JSON: PASS.
  - 김도성 담당 외 132개 파일 mtime: 압축 해제 시점 그대로, 무단 수정 0건.
  - **Pydantic 인스턴스 통합 smoke**: 환경 제약(컨테이너 네트워크 차단 — pip 설치 불가)으로 미실행. stdlib + Fake 클래스 시뮬레이션으로 normalize/disp/매핑 로직 모두 PASS, 실제 Pydantic ValidationError 검증은 운영 환경에서 별도 수행 필요. 섹션 1·2·3·4·5와 동일한 환경 제약 보고 유지.
- 현재 담당 파트 진척도: 50% (12섹션 중 6섹션 완료, 진척도 산정 원칙 §7.4 "30~45% 핵심 모듈 1차 구현 완료"에서 한 단계 위 진입 — 섹션 6은 김도성 4월 핵심 normalize 보강이라 50% 도달이 합리적)
- 다음 섹션 진행 가능 여부: 가능. 섹션 7(섹션 6 산출물 검토 및 패치) 진입 가능. 검토 섹션이므로 사용자의 명시적 지시 없이 자동 진행하지 않음.
- 다음 섹션에서 해야 할 일:
  - 섹션 7 — normalize.py·_normalize_arrivals·low_floor_filter·README §4·선행작업의존성 정리.md 갱신 검수. 매핑 표 1차 출처 충실도, 단위 테스트 커버리지 적정성, banker's rounding 인지 사항이 docstring에 충분히 노출됐는지, 빈 응답 정책이 외부 호출자 관점에서 적절한지, 충돌 이슈 미발생 재확인. `김도성_디버그 리포트.md`에 DEBUG-김도성-0003 작성.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 6 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함
- 관련 의존성 ID: DEP-APR-003 (김도성 섹션 6,7 → 심현석 섹션 10,11), DEP-FUT-004 (4월 핵심 모듈 통합 → 야외 필드 테스트)
- 내 섹션이 선행 섹션인지/후행 섹션인지: **선행 섹션** (DEP-APR-003 선행 둘 중 하나, DEP-FUT-004 선행 4개 중 하나)
- 선행 섹션 상태: 김도성 섹션 6 → (구현완료_2026-05-07 18:35) 두 위치 모두 갱신
- 실제 구현물 2차 확인 결과: `_normalize_arrivals` 본격 구현 + `low_floor_filter` 보강 + `normalize` 헬퍼 3종 + `BusArrivalsService` 빈 응답 정책 — 후행 심현석 섹션 10 백엔드가 `BusArrivalsService.get_arrivals(stop_id)` 호출만으로 표준 응답을 받을 수 있음. 매핑 헬퍼 3종은 단위 테스트 커버. 단 실제 호출은 mock 모드 한정.
- docs/rw/선행작업의존성 정리.md 최신화 여부: 김도성 섹션 6 항목 두 위치 모두 갱신, 다른 의존성/팀원 섹션은 손대지 않음.
- 후행 작업 가능 여부: 김도성 섹션 6 단독으로는 DEP-APR-003 선행 둘 중 하나만 충족. 김도성 섹션 7(검토/패치) 완료 후 두 선행이 모두 충족된다. 심현석 에이전트는 자기 섹션 1~9 순차 완료 후 섹션 10에서 본 산출물을 사용 가능. DEP-FUT-004는 다른 선행 3개(윤현섭/심현석/안준환 섹션 10)도 필요하므로 본 갱신만으로 후행 진입은 불가.

### 기록 0007

- 기록자: 김도성의 에이전트
- 기록일시: 2026년 05월 07일 18시 58분 (KST)
- 완료 섹션: 섹션 7
- 섹션 유형:
  - [ ] 사전 검토
  - [ ] 직접 구현
  - [x] 검토/패치
  - [ ] 최종 심층검증
- 이번 섹션의 목표:
  - 섹션 6 산출물(normalize.py 신규, bus_arrivals_service.py·low_floor_filter.py·__init__.py·README §4·선행작업의존성 정리.md DEP-APR-003+DEP-FUT-004 갱신, 공통 진행사항 기록 0006)을 검수하고 잘못된 패치는 해당 부분만 보정한다. `김도성_디버그 리포트.md`에 DEBUG-김도성-0003을 작성하고, 충돌 가능성이 있으면 docs/rw/충돌 이슈.md에 기록한다.
- 이번 섹션에서 수행한 작업:
  - 검수 6개 항목 수행. 1·2·4·5·6은 PASS. 검수 3에서 섹션 6 patch 잘못 1건(보통 심각도, docstring 영역) 발견 → 보정 완료. 충돌 이슈 신규 등록 0건(검수 6 결과).
  - 검수 1: normalize.py·README §4·§5의 매핑 표가 1차 출처(서울 BIS Open API 공식 레퍼런스)와 일치 — busType 0/1/2, reride_Num 0/3/4/5, URL 인용 모두 PASS.
  - 검수 2: 매핑 헬퍼 3종 추가 edge case 23건(bool/float/zero-padded/문자열/매핑 외 코드/3600초/86400초/'1e2'/banker's rounding 양쪽) 검증 — 모두 PASS. 정상 입력 정확 + 비정상 입력 안전 흡수.
  - 검수 3: **섹션 6 패치 잘못 발견** — `seconds_to_arrival_minutes` docstring이 30초 → 0분만 명시하고 90초 → 2분(banker's rounding 다른 한쪽) 누락. "banker's rounding" 핵심 용어 자체가 docstring에 없어 사용자가 운영 디버깅 시 검색 keyword 확보 불가.
  - 패치 1건 수행: `normalize.py`의 `seconds_to_arrival_minutes` docstring 보강. "Python 내장 round() — banker's rounding (round half to even)" 명시, 9개 케이스 표(20/29/30/31/60/89/90/91/180초) RST 형식 추가하여 양쪽 banker's 거동(30초→0분, 90초→2분) 모두 노출. 호출자가 자체 변환 사용할 수 있다는 안내 명시. 함수 본체는 변경하지 않음.
  - 검수 4: `BusArrivalsService.get_arrivals` try/except 본문 분석 — `PublicDataEmptyResponseError` catch ✓, `ServiceKeyMissing`/`Network` propagate ✓, `PublicDataError` 부모 catch 없음 ✓ (자식 모두 흡수되는 위험 회피), `empty_response` 정합성 PASS.
  - 검수 5: `_normalize_arrivals` 견고성 추가 10 시나리오(빈 list/staOrd 음수·float string/exps1=0/exps1 누락+kals1 fallback/모든 노선 식별 None/모든 필드 None/1000건 성능 5.5ms/generator 입력) 모두 PASS.
  - 검수 6: 충돌 이슈.md와 다른 팀원 영역 점검 — shared_contracts 변경 0건, 다른 팀원 소유 경로 수정 0건, API 계약 변경 0건, 4월 범위 침범 0건 → 충돌 이슈 신규 등록 불필요. 단 `_call_arrivals_api` 본격 구현 시점(섹션 8/10 후보)에 심현석 백엔드와 호출 인터페이스 협의 필요 — 현재는 잠재 협의 사항이지 충돌 아님.
- 수정/생성한 주요 파일:
  - `services/public_data/public_data_client/normalize.py` (docstring 보강 패치, 함수 본체 변경 없음)
  - `docs/rw/김도성_디버그 리포트.md` (DEBUG-김도성-0003 추가)
  - `docs/rw/공통 진행사항.md` (본 기록 0007)
  - `docs/rw/선행작업의존성 정리.md` (DEP-APR-003 김도성 섹션 7만 (미구현) → (구현완료_2026-05-07 18:58))
- 이전 섹션에서 언급된 이슈:
  - (섹션 6 인지) `seconds_to_arrival_minutes` banker's rounding 거동 — docstring 보강으로 처리.
  - (섹션 6 이월) `_call_arrivals_api` 본격 구현은 운영 환경 제약(TAGO 명세 미확보 + 서울 BIS 인증키 발급 불가)으로 섹션 8/10 후보 유지.
  - (섹션 6 이월) TAGO vehicletp/혼잡도 매핑 명세 직접 확인은 동일 운영 제약으로 섹션 8/10 후보 유지.
- 이전 이슈 처리 결과:
  - 섹션 6 인지 사항 1건 처리 완료(docstring 보강). 운영 제약 이월 2건은 섹션 8/10 후보 유지.
- 이번 섹션에서 새로 발견한 이슈:
  - (인지 — 패치 잘못 아님) `map_vehicle_type_to_low_floor`에 `1.0`(float) 입력 시 `str(1.0)='1.0'`이라 매핑 안 됨 → default. 공공데이터 응답은 XML/JSON 모두 문자열 '0'/'1'/'2'이라 실제 호출에서 발생 안 함.
  - (인지 — 책임 분리 보존) 모든 노선 식별자(rtNm/busRouteId/busRouteAbrv) None인 raw_item은 `routeId="", busNo=""`로 normalize. Pydantic 빈 문자열 허용하지만 실용상 비정상. 응답 정상성 검증은 호출자 책임이라 `_normalize_arrivals`가 추가 raise하면 책임 분리 위반 → 현재 정책 보존.
  - (잠재 협의 사항 — 충돌 아님) `_call_arrivals_api` 본격 구현 시점에 심현석 백엔드와 호출 인터페이스 협의 필요.
- 관련 충돌 이슈: 없음 (검수 6 결과). 충돌 이슈.md 등록 불필요.
- 작성/최신화한 문서:
  - docs/rw/김도성_디버그 리포트.md (DEBUG-김도성-0003)
  - docs/rw/공통 진행사항.md (김도성 기록 0007)
  - docs/rw/선행작업의존성 정리.md (DEP-APR-003 김도성 섹션 7 → 구현완료)
- 테스트 또는 검증 결과:
  - JSON parse: 17/17 PASS.
  - Python AST parse: 32/32 PASS (패치 후에도 syntax 오류 없음).
  - 매핑 헬퍼 3종 추가 edge case 23건: 모두 PASS.
  - `_normalize_arrivals` 추가 견고성 10 시나리오: 모두 PASS, 1000건 normalize 5.5ms로 성능 적정.
  - `BusArrivalsService.get_arrivals` 빈 응답 정책 본문 분석: PASS.
  - 충돌 이슈 점검: 0건 신규 등록.
  - shared schema ↔ mock JSON: PASS.
  - `__init__.py` export 정합성: 16/16 PASS.
  - 김도성 담당 외 132개 파일 mtime: 압축 해제 시점 그대로 — 무단 수정 0건.
  - 다른 팀원 섹션 상태 무단 변경 0건 (선행작업의존성 정리.md re-check).
  - **Pydantic 인스턴스 통합 smoke**: 환경 제약(컨테이너 네트워크 차단)으로 미실행. 매핑 헬퍼 3종 + `_normalize_arrivals` + 빈 응답 정책 모두 stdlib + Fake 클래스 시뮬레이션으로 PASS, 실제 Pydantic ValidationError 검증은 운영 환경에서 별도 수행 필요. 섹션 1·2·3·4·5·6과 동일 환경 제약 보고 유지.
- 현재 담당 파트 진척도: 58% (12섹션 중 7섹션 완료, 진척도 산정 원칙 §7.4 "50~65% 핵심 모듈 검토 통과" 진입)
- 다음 섹션 진행 가능 여부: 가능. 섹션 8(AI 비전 데이터셋 계획·라벨링 기준·모델 후보 보강) 진입 가능. 검토 섹션이므로 사용자의 명시적 지시 없이 다음 구현 섹션으로 임의 진행하지 않음.
- 다음 섹션에서 해야 할 일:
  - 섹션 8 — `ai_vision/dataset_plan/`에 데이터 수집 가이드, 개인정보 보호 기준, 라벨링 일관성 규칙 보강. `ai_vision/model_research/`에 후보 모델 비교 표 보강(YOLOv8n/YOLOv11n/MobileNet/EfficientDet-Lite — 입력 해상도/추론 위치/모바일 적합성 기준). `ai_vision/pipelines/`에 향후 통합 위치 메모. 4월 범위에서 학습/추론 코드 작성 금지 원칙 유지. 비콘/공간음향 영역은 김도성 4월 담당 아님 — 절대 손대지 않음.
- 사용자에게 전달한 GitHub 업로드 권유 문구:
  - **지금 섹션 7 끝났습니다. 캡스톤 과목 점수 획득 및 충돌 병목 최소화를 위해 지금까지 진행상황 깃헙에 올리시는 것을 권유드립니다.**

#### docs/rw/선행작업의존성 정리.md 기록 연동
- docs/rw/선행작업의존성 정리.md 확인 여부: 확인함
- 관련 의존성 ID: DEP-APR-003 (김도성 섹션 6,7 → 심현석 섹션 10,11). DEP-FUT-004는 김도성 섹션 6만 선행으로 등록되어 있어 섹션 7 본 변경과 직접 관련 없음(섹션 6에서 이미 갱신 완료).
- 내 섹션이 선행 섹션인지/후행 섹션인지: **선행 섹션** (DEP-APR-003 선행 둘 중 하나)
- 선행 섹션 상태: 김도성 섹션 7 → (구현완료_2026-05-07 18:58) 갱신. DEP-APR-003 선행 둘(섹션 6, 7) 모두 (구현완료_)로 충족됨.
- 실제 구현물 2차 확인 결과: normalize 헬퍼 3종 + `_normalize_arrivals` 본격 + low_floor_filter 보강 + 빈 응답 정책 + docstring 명확화 — 모두 검수 통과. 후행 심현석 섹션 10 백엔드가 본 산출물을 안정적으로 사용 가능.
- docs/rw/선행작업의존성 정리.md 최신화 여부: 김도성 섹션 7 항목만 갱신, 다른 의존성/팀원 섹션 무손상. DEP-FUT-004는 김도성 섹션 7이 선행으로 등록되어 있지 않으므로 본 변경 대상 아님.
- 후행 작업 가능 여부: DEP-APR-003 선행 두 개 모두 충족됨 → 심현석 에이전트는 자기 섹션 1~9 순차 완료 후 섹션 10·11에서 김도성 산출물 직접 사용 가능. 단 김도성 섹션 6의 `_call_arrivals_api` 본격 구현은 미완이므로 mock 모드 한정 — real 모드 의존이 필요한 심현석 단위 통합 테스트는 섹션 8/10에서 김도성 측 보강이 추가로 필요할 수 있다(잠재 협의 사항).

---

## 9. 초기 상태

```txt
문서 생성 상태: 초기 템플릿 생성 완료
현재 등록된 진행 기록: 없음
사용 시작 시점: 각 팀원의 AI 에이전트가 섹션 1을 시작하는 시점
```

이 문서는 프로젝트 초기 문서 생성 단계에서는 단순 템플릿으로 유지하고, 실제 팀원별 개발 섹션이 시작된 뒤부터 본격적으로 최신화한다.

---

## docs/rw/선행작업의존성 정리.md 기록 연동 규칙

각 에이전트는 섹션 종료 시 자신의 작업이 `docs/rw/선행작업의존성 정리.md`와 관련 있는지 확인하고, 관련이 있다면 자기 팀원 기록 공간에 반드시 다음 항목을 남긴다.

```txt
- docs/rw/선행작업의존성 정리.md 확인 여부:
- 관련 의존성 ID:
- 내 섹션이 선행 섹션인지/후행 섹션인지:
- 선행 섹션 상태:
- 실제 구현물 2차 확인 결과:
- docs/rw/선행작업의존성 정리.md 최신화 여부:
- 후행 작업 가능 여부:
```

자신이 구현한 섹션이 선행 섹션으로 명시되어 있는데 이 문서를 최신화하지 않았다면, 해당 섹션은 후행 에이전트 기준에서 완료된 것으로 보지 않는다.
